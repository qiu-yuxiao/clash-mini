# Clash Mini 待处理 Bug 跟踪列表 (Pending Bug Tracking List)

本文件是 Clash Mini 项目专门用于记录、跟踪和闭环所有发现的系统缺陷（Bugs）的官方文档。每当用户反馈新 Bug 或开发过程中发现故障时，必须第一时间将其记入本文件，并在修复后予以更新。

---

## 📌 待处理 Bug 列表 (Pending Bugs)

| Bug 编号 | 缺陷描述与现象 | 目标版本 | 目前状态与说明 |
| :--- | :--- | :---: | :--- |
| **BUG-017** | 置顶当前活跃出口节点状态条无法正确加载并渲染代理节点的协议与 IP:port，退化为 Direct 或空白。 | v1.1.4 | **待处理**：由于后台默认禁用外部 HTTP REST 端口（改用本地 Pipe/Socket IPC 通信），前端直接使用 `fetch()` 访问该端口的逻辑失效。已确定更优雅的解决方案：前端的 `activeNodeRecord`（从 Tauri IPC 管道定期同步）中已包含运行时 `server` 与 `port` 属性，无需进行外部 HTTP 请求。可以通过 `useMemo` 直接从本地 `activeNodeRecord` 中提取这些字段，废除 `fetch` 代码即可恢复正常渲染。 |
| **BUG-027** | 在路径控制下右键点击链接目标，选择“设为全局直连”或“设为代理分流”无实际分流改变（但会弹出已置顶生效提示）。 | v1.1.4 | **待处理**：由于右键回调逻辑中优先使用进程名 `metadata.process`（如 `chrome.exe`）建立规则，导致域名匹配 `metadata.host` 被完全屏蔽。这导致操作实际改变了整个浏览器的网络分流而非单特定网址。需要调整判断优先级，优先处理 `metadata.host` 等目标，最后兜底进程。 |
| **BUG-028** | 客户端在开机自动启动后，无法正常自动刷新订阅节点（导致无可用节点或节点过期），代理功能瘫痪，必须人工手动点击更新。 | v1.1.4 | **待处理**：由于开机自启时系统网络接口（如 Wi-Fi、网卡 DHCP）通常尚未就绪，定时任务模块在启动 5 秒后检测到过期任务即立刻发起 HTTP 订阅更新并因断网失败。更新失败后未设任何重试机制，直接跳入 24 小时后的下一周期，造成客户端空有自启却无节点可用。需要增加断网重试机制（例如检测网络连接状态，或在失败时采用指数退避重试）。 |
| **BUG-029** | 当前活跃出口节点状态栏的元素排列顺序不合理（从左到右依次为：协议类型、主标签文本、节点名称、延迟、IP端口），视觉阅读逻辑混乱。 | v1.1.4 | **待处理**：当前左中右三段式布局将主标签“当前活跃出口节点：”夹在“协议类型”与“节点名称”中间，且两侧宽度不对称。计划调整为流式阅读布局（主标签 ➜ 协议标签 ➜ 节点名称 ➜ 延迟 ➜ 端口信息）。同时在窗口缩小/响应式断点上：最先隐藏无实质信息量的主标签，其次隐藏协议标签，最后隐藏端口，只保留最核心的“节点名称”与“延迟数值”。 |
| **BUG-030** | 程序初次启动或重载时，即使配置文件默认开启了“系统代理”，流量接管模式的 UI 滑块依然显示为“手动模式”。 | v1.1.4 | **待处理**：前端 `_layout.tsx` 中使用 OS 实时的系统代理状态 `systemProxyIndicator` 来驱动接管模式滑块。由于启动时前端渲染与后端异步设置代理（`init_system_proxy`）存在时序竞态，前端首次查询时 OS 代理尚未设置完成返回 `false`，导致滑块错位显示为“手动模式”且无法自动刷新。修复方案：将滑块的驱动数据源由 OS 实时状态 `systemProxyIndicator` 修改为应用的配置状态 `verge.enable_system_proxy`（即 `configState`），符合主流代理软件逻辑，避免时序竞态和启动闪烁。 |
| **BUG-031** | 在开启系统代理时修改 Mixed Port，保存后流量接管模式滑块会自动跳回手动模式，且系统代理端口未同步导致断网。 | v1.1.4 | **待处理**：由于修改 Mixed Port 时后端仅响应了 `RESTART_CORE` 重启内核，却未给 `mixed_port` 变更绑定 `SYS_PROXY` 标识，导致 Windows 注册表中的代理端口未同步更新（仍为旧端口），引起断网。同时，前端因比对新端口与注册表旧端口不匹配，导致状态检测 `systemProxyIndicator` 返回 `false`，使得滑块倒退回“手动模式”。修复方案：1. 后端：在 `feat/config.rs` 中为 `mixed_port` 变更追加 `UpdateFlags::SYS_PROXY` 标记，确保其自动调用 `update_sysproxy()` 同步修改系统代理端口；2. 前端：同 `BUG-030` 修复，改为使用配置状态驱动滑块。 |
| **BUG-032** | 在不同流量接管模式（如系统代理与 TUN 模式）之间切换时，滑块会先跳回“手动模式”再滑至目标模式，造成视觉闪烁和困惑。 | v1.1.4 | **待处理**：前端 `handleTakeoverModeChange` 逻辑中，切换模式是分步非原子执行的（例如切换到 TUN 时，先调用 `toggleSystemProxy(false)` 关闭代理，这会立即将 `systemProxyIndicator` 改为 `false` 触发前端渲染更新为“手动模式”；随后再在下一阶段去 patch 开启 TUN）。如果是首次开启 TUN 还需要等待网卡驱动服务安装，中间的“手动模式”会停留数秒。修复方案：将分步的非原子调用全部重构为单次原子操作。通过 `patchVerge` 同时下发 `{ enable_system_proxy: false, enable_tun_mode: true }`，对于需要安装服务的，在保持当前模式状态下完成安装后再一并下发配置，杜绝中间态闪烁。 |
| **BUG-033** | 导入 3 个或更多订阅链接时，订阅列表没有滚动条，导致用户无法察觉可滚动的剩余配置项。 | v1.1.4 | **待处理**：由于全局样式 `index.scss` 和 `use-custom-theme.ts` 中通过 `* { scrollbar-width: none !important; }` 和 `*::-webkit-scrollbar { display: none !important; }` 强力隐藏了所有滚动条，导致侧边栏订阅列表（maxHeight 110px，超限后）等滚动区域没有任何视觉提示。修复方案：废除粗暴的全局通配符隐藏，改用精细化的全局 3D 窄滚动条样式（6px 宽，配合已有的 `--scrollbar-bg` 和 `--scrollbar-thumb` 变量进行自适应着色），仅对真正不需要滚动条的纯容器（如 body）实施 `overflow: hidden`。⚠️ **特殊约束**：主页和设定页的两张类 Excel 风格表格必须保持“可滚动但完全隐藏滚动条”的既有视觉设计，需要在表格组件局部通过 CSS（如 `scrollbar-width: none`）进行显式覆盖，避免全局修复导致其滚动条外露。 |
| **BUG-034** | 激活新导入的订阅或切换不同订阅时，系统不会自动进行首次延迟测速、自动排序节点并自动挑选最快节点，导致用户必须手动操作，且无序的列表视觉体验极差。 | v1.1.4 | **待处理**：当激活或切换配置后，Clash 核心重新载入，节点延迟和可用性未知。若不进行自动测速和排序，用户看到的节点列表是无序的，且最快的节点可能埋没在列表中，视觉体验杂乱。修复方案：在 `_layout.tsx` 中监听 `currentProfileUid` 变化并在 `enhanceProfiles` 重载成功后自动执行：1. **首次自动测速**：若包含 `proxyProviders` 则后台执行 `healthcheckProxyProvider(name)`，inline 配置则执行 `delayManager.checkListDelay`；2. **自动排序置顶**：测速完成后，将主页表格排序状态自动切换为延迟排序（即 `sortType: 1`），让延迟最低（最快）的节点在视觉上置顶；3. **自动选择最快**：过滤出延迟大于 0 且最小的健康节点，调用 `changeProxy` 激活它，实现“一键导入 ➜ 自动测速 ➜ 排序置顶 ➜ 自动连上最快”的极致体验。 |




---

## 📌 已解决的历史 Bug (Resolved Historical Bugs)

| Bug 编号 | 缺陷描述与现象 | 解决版本 | 修复方法与说明 |
| :--- | :--- | :---: | :--- |
| **BUG-024** | Windows 标题栏数学粗体字符在部分 Windows 系统下显示为方块或乱码。 | v1.1.2 | 废除了 Unicode 数学符号映射转换，将标题栏文字恢复为标准的 ASCII 字符（例如 `"Clash Mini Ver.1.1.1"`），保证英文字符在所有 Windows 版本上正常呈现。 |
| **BUG-025** | 滑块选项文本只有在被选中时才是粗体，与普通按钮全部加粗不统一。 | v1.1.2 | 在 `_layout.tsx` 中将三个三选一滑块的所有文本选项字重（fontWeight）常驻设置为 `'bold'`，无论是否处于选中状态均保持一致加粗。 |
| **BUG-026** | 界面控制尺寸显得过于偏大，尤其是主按钮、双滑动条和三选一滑块。 | v1.1.2 | 执行了控件尺寸微缩精细调整：主按钮（导入订阅和运行日志）统一调整至 `24px`；双滑动条轨道高度与滑珠直径缩减至 `10px`；三个三选一滑块高度统一调至 `26px`；路径控制 of 二选一滑块和断开/清空按钮统一调至 `24px`；小开关保持 `14px` 不变，实现极致紧凑拟物效果。 |
| **BUG-018** | 底部指标卡片整体偏大，且在折行及小屏状态下字体比例稍显失调。 | v1.1.2 | 将 Metrics Row 长条高度缩减为 26px，卡片高缩减为 22px，图标大小调至 16px，数字字号设为 15px，单位字号设为 10px，并将“上传:”、“总量:”、“下载:”、“总量:”这 8 个汉字标签字号统一调整为紧凑的 11px，其余 padding 同步调小，完美对齐紧凑精致排版。 |
| **BUG-016** | 分流策略倾向切换到“代理兜底 (Proxy Fallback)”时系统完全无法联网，且局域网连接及本地回环被错误代理。 | v1.1.1 | 在 `enforce_mini_agreements` 规则重构流程中，在末尾 `MATCH` 规则之前注入局域网直连规则及国内流量（CN）直连规则，从而打破 DNS/DoH 解析循环依赖死锁，解决联网及局域网互访故障。 |
| **BUG-015** | 配置文件增强与激活逻辑在 `_layout` 的 `useEffect` 中与未缓存的 `activateSelected` 发生联动，导致启动和重载后陷入无限循环重新加载并弹窗报错“配置校验失败”（在 v1.0.6 版本中随其他功能修改被意外还原）。 | v1.0.9 | 再次从 `_layout.tsx` 的 `useEffect` 依赖项中移除 `activateSelected`，仅保留 `currentProfileUid`，并在行尾添加 eslint 禁用注释，彻底斩断无限更新环，防止后续自动修复格式化时被再次加入。 |
| **BUG-014** | 在全局代理（Global）模式下，用户选择节点无法控制流量出口（节点选择脱节）或启动/重载后断网的问题。 | v1.0.4 | 在启动、配置激活 and 模式切换时，强制将内核 GLOBAL 组绑定指向唯一代理组 PROXY，确保全局出口与用户界面选中的节点保持 100% 同步一致。 |
| **BUG-002** | 默认开启 IPv6 且缺乏界面控制，可能导致某些网络环境下 DNS 泄露或代理分流异常。 | v1.0.3 | 在 `clash.rs` 默认模板中将 `ipv6` 设为 `false`；在 `tun.rs` 逻辑中，当 `ipv6` 字段未定义时降级为 `false`。 |
| **BUG-003** | DNS 覆写默认未开启且缺乏自动优化的评估与改造待办，导致国内直连和防泄露体验不佳。 | v1.0.3 | 将 `enable_dns_settings` 默认值设为 `true`；初始化时如果 `dns_config.yaml` 不存在，则自动写入一份深度优化的 DNS 覆写配置。 |
| **BUG-011** | 默认未开启代理守护（Proxy Guard）导致系统代理易被篡改或静默失效。 | v1.0.3 | 将 `enable_proxy_guard` 默认值设为 `true`，以在后台自动校验并修复系统代理。 |
| **BUG-012** | 默认未开启自动轻量化模式（Memory Optimization）导致后台挂机占用内存较高。 | v1.0.3 | 将 `enable_auto_light_weight_mode` 默认值设为 `true`，自动轻量化延迟 `auto_light_weight_minutes` 默认缩短为 `5` 分钟，在最小化挂机时自动销毁 WebView 进程，节省内存。 |
| **BUG-013** | 自动检查更新选项开启（auto_check_update）引发的冗余后台行为。 | v1.0.3 | 将 `auto_check_update` 默认值设为 `false`，彻底停用冗余的自动检查更新机制。 |
| **BUG-001** | 开机自启非管理员权限下修改后台静默失败且前端 Switch 状态脱节 | v1.0.2 | 前端 PATCH 失败时执行回滚回刷；后端新增 `create_task_elevated` 和 `remove_task_elevated`，在非管理员权限下注册或删除任务时，通过 Windows `runas` 完美唤起 UAC 提权并完成操作。 |
| **BUG-004** | 代理模式在非规则模式（如全局、直连）下隐藏表头导致定位、延迟排序按钮丢失 | v0.2.0 | 修改前端组件，使表头操作行在「全局代理」和「全局直连」下保持常驻显示。 |
| **BUG-005** | 表格最外侧左、右竖边框非 5px double 且左侧表格线缩进空缺十几个像素 | v0.2.0 | 修正表格组件左右纵向边框为双线；调整左边距偏移实现完美闭合。 |
| **BUG-006** | 界面清除毛玻璃不彻底，部分 Aero 样式变量残留导致滚动漏光重叠 | v0.2.0 | 完全移除了全局 SCSS 和 React 组件中关于 rgba 半透明、Mica、Acrylic 以及 Aero 变量的引用，全部采用 100% 实体纯色和 MUI 语义化颜色。 |
| **BUG-007** | 订阅管理中偶现没有名字的空行订阅项，重启后仍反复出现 | v0.2.0 | 前端渲染解析时强制对空名称的订阅项进行过滤剔除。 |
| **BUG-008** | 设置页面右上角设置齿轮按钮与底部的节点名称/延迟重合且无法点击 | v0.2.0 | 重新调整了齿轮按钮 of 固定定位布局（36x36），并为下方活跃出口节点栏右侧增加 44px 避让间距，防止视觉重合。 |
| **BUG-009** | 启动时提示有 Clash Verge 新版本并引导升级从而覆盖客户端的问题 | v0.0.1 | 彻底废除并禁用了检查官方版本更新的机制与升级 API 接口。 |
| **BUG-010** | 便携版绿色压缩包在 Release 时丢失或包含旧版本二进制的问题 | v0.3.2 | 修改 `portable.mjs`，确保在 Tauri 生产编译（Exit Code 0）完全结束后方运行打包逻辑。 |

---

## 📌 待优化与设计微调 (UI/UX Enhancements)

| 优化编号 | 改进项与现象描述 | 目标版本 | 目前状态与设计方案 |
| :--- | :--- | :---: | :--- |
| **STYLE-001** | 按钮与滑块立体感（3D 物理凹凸质感）较弱，缺乏极具质感的拟物视觉反馈。 | v1.1.4 | **待设计**：计划全面升级拟物和 3D 设计细节。可参考 [Neumorphism.io](https://neumorphism.io/#e0e0e0) 软拟物阴影技术。1. **按钮**：采用多重重叠光源阴影（通过多层 `inset` 阴影模拟光照倒角 and 暗面，或通过对角线双重高光阴影呈现软凸起/凹陷感），或引入游戏风格的物理厚底边框（Thick Bevel，点击时整体按压下沉）；2. **滑轨与滑块**：升级滑块轨道内壁的超深内凹阴影（多重 `inset`），滑块表面引入金属拉丝或玻璃质感圆球，并增强色彩发光（Vibrancy）联动时的镜面高光，强化“实体流量监视器”的机械感。 |
| **FEAT-001** | 支持“窗口始终置顶”功能，以便将软件缩小为迷你流量监视器时作为悬浮窗常驻桌面。 | v1.1.4 | **待设计**：1. **后端**：在 `verge.rs` 配置文件结构体中新增 `enable_always_on_top: Option<bool>` 字段；在窗口创建初始化（`init_window`）以及更新配置（`patch_verge`）时动态调用 Tauri 底层 `window.set_always_on_top(enabled)` API；2. **前端**：在侧边栏“基础设置”中添加“窗口始终置顶”的 Switch 开关；同时在顶部栏右上角设置齿轮的左侧，新增一个精致的“置顶图钉 (Pin)”快捷按钮，启用时图钉倾斜并亮起主题色霓虹发光，支持一键锁定/解锁置顶。 |
| **FEAT-002** | 侧边栏“Mixed Port”端口输入栏去除右侧的“保存磁盘”小按钮，升级为更智能现代的失焦及回车自动保存机制。 | v1.1.4 | **待设计**：前端修改 `_layout.tsx`，将 Mixed Port 端口文本框旁边的 `IconButton`（磁盘图标）彻底移除，腾出界面空间；在 `TextField` 上绑定 `onBlur={handleSavePort}` 实现输入框失焦后自动进行端口可用性校验并保存；同时绑定 `onKeyDown` 监听回车键，在按下回车键时触发 `handleSavePort` 并调用 `target.blur()` 主动使输入框失焦，提升操作流畅性。 |



