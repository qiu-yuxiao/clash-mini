# Clash Mini 待处理 Bug 跟踪列表 (Pending Bug Tracking List)



本文件是 Clash Mini 项目专门用于记录、跟踪和闭环所有发现的系统缺陷（Bugs）的官方文档。每当用户反馈新 Bug 或开发过程中发现故障时，必须第一时间将其记入本文件，并在修复后予以更新。



---



## 📌 待处理 Bug 列表 (Pending Bugs)



| Bug 编号 | 缺陷描述与现象 | 目标版本 | 目前状态与说明 |

| :--- | :--- | :---: | :--- |
| **BUG-047** | 在本地提交推送（`git push`）时，Git 的 `pre-push` 钩子运行 `cargo clippy --all-targets` 遇到测试模块中的 `.unwrap()` 报错导致推送失败（由于项目全局禁用了 `unwrap_used`）。 | v3.0.1 | **已解决，已确认**。<br>**设计要求**：在 `universal_parser.rs` 的测试模块 `mod tests` 前端声明 `#[allow(clippy::unwrap_used)]`，允许在测试中合理使用 `.unwrap()` 以提取结果，同时修复该测试中的冗余借用警告（如 `&serde_yaml_ng::Value` 修改为直接传值）。<br>**代码状态**：代码修改完成，静态编译及单元测试全部通过，并成功推送。 |

| (暂无) | 所有当前版本 (v2.0.1 followup) 发现 of 缺陷均已解决。 | - | - |



---



## 📌 已解决的历史 Bug (Resolved Historical Bugs)



| Bug 编号 | 缺陷描述与现象 | 解决版本 | 修复方法与说明 |
| **BUG-050** | Clash Mini 的 CPU 资源消耗（约 13%~15%）显著高于 Clash Verge（约 3%）。主要原因在于：1. 迁移后的 Connections 表单与 Logs 日志窗口均被常驻挂载且始终通过 WebSocket 在后台以极高频率（16ms - 50ms）解析 JSON 报文并进行状态差异对比和 UI 渲染调度，即使相关 Drawer（设置抽屉）或 Dialog（日志弹窗）处于关闭状态；2. 流量数据 Hook（useTrafficData）未响应窗口可见性（pageVisible），导致最小化或后台运行时仍持续进行 WebSocket 通信与渲染。 | v3.0.3 | **已解决，已确认**。<br>**设计要求**：1. 为 `useConnectionData` 引入 `enabled` 参数；在 Layout 挂载中绑定 `enabled: drawerOpen`；2. 在 `useConnectionData` 内部实现双模切换：如果处于关闭状态但可见，则停止 WebSocket 通信，降级为每 3 秒使用 REST 接口（getConnections）轮询 totals 且不合并处理 connection list；若不可见，则彻底关闭轮询；3. 在 `_layout.tsx` 中对 `<LogsPage />` 实行条件渲染 `{logsOpen && <LogsPage />}`，使日志弹窗关闭时自动注销 WebSocket；4. 修正 `useTrafficData` 中的 WebSocket 键值逻辑，使不可见时自动断连；5. 将极窄模式流量折线图线宽统一为 2.5px。<br>**代码状态**：已重构 hooks 和 layout 逻辑，成功通过静态类型安全与 eslint 检查，并在 Actions 云端编译成功打包。 |

| **BUG-049** | 活跃出口节点活性监控定时检测（自动测速与切换）在运行中失效，原因为其 useEffect 依赖了频繁变动的 proxies 状态与 triggerAutoSelectFastestNode，导致定时器在倒计时完成前反复被重置清除，无法正常在后台运行测速调度。 | v3.0.2 | **已解决，待确认**。<br>**设计要求**：将活性监控 `useEffect` 中的外部状态依赖解耦，仅依赖于 `currentProfileUid`；使用 stable Refs 缓存 `proxies` 和 `triggerAutoSelectFastestNode`，使后台定时器能在长周期内平稳不间断运行，达到真正的 60 秒定期测速与失效自动连切。<br>**代码状态**：已重构 `_layout.tsx` 监控计时器逻辑，利用 `proxiesRef` 与 `triggerAutoSelectFastestNodeRef` 隔离渲染重绘，实现后台检测闭环。静态检查与前端静态资源编译通过。 |

| **BUG-048** | 无边框模式下窗口边缘磁吸及自适应吸附与脱离功能丢失，拖拽时缺乏物理吸附感与防自激锁保护。 | v3.0.1 | **已解决，已确认**。<br>**设计要求**：1. 设计 useWindowSnap 状态防抖与 cooling 机制；2. 增加 mousedown 事件的预缓存，消除 onMoved 中的异步 Tauri IPC 获取，保障性能；3. 适配 DPI 缩放物理像素及多显示器。<br>**代码状态**：1. 完成 `useWindowSnap.ts` 的编写，通过 mousedown/mouseup 事件实现首尾阶段的 monitor/window 尺寸预缓存，有效规避高频 onMoved 时的 IPC 消耗；2. 引入 18 像素 (DPI-scaled) 贴合与 24 像素挣脱阈值，实现真实的“磁吸与挣脱”触感并配以 150ms 的防自激锁；3. 成功集成至 `WindowProvider` 的无边框判断逻辑中，并通过本地前端与类型安全编译。 |

| **BUG-046** | 在隐身(stealth)模式下，单击窗口除交互控件和避让区之外的任意区域无法正常恢复原生标题栏（点击唤醒失效），原因为在 `mousedown` 立即启动拖拽会导致 OS 吞掉后续 click 事件。 | v3.0.0 | **已解决，已确认**。<br>**设计要求**：隐身模式下将拖拽启动移至 `mousemove`（鼠标左键按下且移动位移 > 5px），点击唤醒判定改在 `mouseup`（位移 < 5px 且未发生过拖拽），从而避免 click 事件被 OS 吞掉。同时，排除避让区域的唤醒。<br>**代码状态**：已完成 `window-provider.tsx` 逻辑重构，将拖拽启动延迟到 mousemove 位移大于 5px，并通过 mouseup 处理位移小于 5px 时的单击唤醒。已成功通过验证并提交。 |

| **BUG-045** | 在最窄窗口状态下，底部的流量图和数据卡片不够紧凑，且 Canvas 折线图存在缩放模糊。 | v2.0.7 | **已解决，已确认**。<br>**设计要求**：底部面板总高度缩短至 100px，删除顶底分割线和冗余间隙。精简折线线宽为 1.5px 并优化网格。引入 High-DPR 渲染消除拉伸模糊。窗口最小高度限制 MINIMAL_HEIGHT 缩减至 133.0。<br>**代码状态**：已完成 _layout.tsx、traffic-graph.tsx 和 window.rs 相关代码修改，静态编译 and 本地 dev 物理效果验证均通过。 |

| **BUG-044** | 对齐本地“本地导入节点”卡片高亮激活逻辑，并统一其第二行元数据格式。 | v2.0.7 | **已解决，已确认**。<br>**设计要求**：L_Direct_Imports 卡片在界面上始终高亮激活，元数据第二行左侧展示“节点数: {数量}”或“本地文件”，右侧显示纯日期 “YYYY-MM-DD”。<br>**代码状态**：已完成 _layout.tsx 相关修改，并通过静态检查。 |

| **BUG-043** | 废除手动“兼容模式”滑块开关，升级为全自动链接格式识别（YAML与Base64）与默认并联订阅合并，并在导入失败时安全拦截脏数据。 | v2.0.6 | **已解决，待确认**。<br>**设计要求**：从 UI 移除开关，将输入框及按钮文字恢复为标准文案；后端 prfitem.rs 移除开关配置判断，YAML 校验失败时直接尝试 Base64/URI 解析并对垃圾数据输出自定义中文报错；enhance/mod.rs 中默认开启并联订阅合并编译。<br>**代码状态**：已完成全部修改，经 GitHub Actions 云端成功编译并一键静默发行 Windows 免安装绿色版 v2.0.6，已下载解压至 portable_test/ 目录等待最终验证。 |

| **BUG-041** | 启动或加载页面时，主页节点的表格（ProxyGroups）没有显示，只有手动拉伸窗口后才重新显现。 | v2.0.4 | **已解决，已确认**。<br>**设计要求**：彻底移除 React 中的 `isMinimalHeight` 状态与 resize 高度判定逻辑，节点列表改为无条件渲染，容器随窗口变矮自然裁剪（遮挡）且稳定保持行高 20px 不变形，实现完全免事件监听的原生物理遮挡表现。<br>**代码状态**：已清理 `_layout.tsx` 中全部 `isMinimalHeight` 及其 resize 计算，前端静态编译编译成功。 |

| **BUG-042** | 缺乏对非 YAML 格式订阅链接（如 Base64 编码的 ss://, trojan://, vmess:// 链接列表）的直接支持，无法进行兼容转换和多订阅合并冲突处理。 | v2.0.4-full | **已解决，待确认**。<br>**设计要求**：在“兼容模式”下提供通用 URI 解析管道与多订阅合并冲突处理。支持自动对 `ss://`、`trojan://`、`vmess://` 的单行 URI 或 Base64 编码的订阅源进行 robust 还原解析，并转换为 Clash YAML proxy 列表，同时解决同名节点合并时的命名碰撞，并在前端修剪还原显示。<br>**代码状态**：已编写 Rust 后端 Universal Parser 并与订阅刷新管道对接，前端已整合滑动切换按钮并完成类型和 ESLint 校验。 |

| **BUG-039** | 活跃出口节点长期使用变慢/超时，无法自动切换更快的线路，且定时检测间隔过高或过低不够自适应。 | v2.0.0 | **已解决，已确认**。<br>**设计要求**：正常状态下每60秒检测一次当前节点；前一次失败时才以5秒间隔快速复检，连续3次失败触发自动测速与切换，减少不必要的背景测速开销。<br>**代码状态**：已在 `_layout.tsx` 中编写 60s 正常检测间隔、5s 异常复检间隔及 3 次计数自动触发测速和重置逻辑，并在手动/自动切换节点时自动清零计数器。 |

| **BUG-040** | 一键静默发行预检时强杀冲突进程，导致 host 上的 Clash Verge 被误杀导致 AI 和本地连接网络中断。 | v2.0.0 | **已解决，已确认**。<br>**设计要求**：静默发行预检及进程强制结束机制不得影响 host 正常的代理客户端（`clash-verge.exe` 及中转内核），仅清扫本项目相关的开发残留进程及冲突端口。<br>**代码状态**：更新了 `clash_mini_silent_release.md` 中的检测脚本，仅对 `clash-mini`/`Clash Mini` 或工作区路径进程进行强杀，并只在系统服务二进制路径属于工作区时才处理服务锁，成功保护了 host 外部网络连接。 |

| **BUG-038** | 设置页展开后遮挡了右上角关闭按钮，导致用户无法关闭设置窗口而被锁死。 | v1.1.9 | **已解决，已确认**。<br>**设计要求**：恢复右上角设置齿轮与关闭按钮二合一动态切换的设计（打开时显示 Close 叉号，关闭时显示 Settings 齿轮），实现同物理坐标的顺畅进出，并调整顶部容器 z-index 为 110 防止被遮挡。<br>**代码状态**：已还原二合一动态按钮并调整 header 层级，用户已在 v1.1.6 验证通过。 |

| **BUG-017** | 置顶当前活跃出口节点状态条无法正确加载并渲染代理节点的协议与 IP:port，退化为 Direct 或空白。 | v1.1.7 | **已解决，已确认**。<br>**设计要求**：状态条应当能准确读取本地 Clash 运行 of 代理节点详情（协议、IP:port、延迟等），即使外部 HTTP REST 端口被禁用，也能通过本地 IPC 定期同步获取。<br>**代码状态**：已废弃外部 HTTP 请求。引入了后端自定义 Tauri 命令 `get_proxy_addr`，当有 provider 时匹配解析其配置文件，无 provider 时 fallback 解析 `clash-mini.yaml` 提取其 IP 与端口，并在前端渲染。 |

| **BUG-027** | 在路径控制下右键点击链接目标，选择“设为全局直连”或“设为代理分流”无实际分流改变（但会弹出已置顶生效提示）。 | v1.1.4 | **已解决，已确认**。<br>**设计要求**：在连接日志表格中，对连接行右键选择分流倾向（例如“设为全局直连”或“设为代理分流”）应当精确影响该域名或目的 IP，而不是将宿主进程名加入直连，从而避免导致整个浏览器被全局绕过。<br>**代码状态**：已在 `connection-table.tsx` 快捷分流规则生成逻辑中，优先基于 `metadata.host` 域名生成规则，其次基于 IP，最后仅在无法获取网络信息时进行进程名 `metadata.process` 的兜底，用户验证通过。 |

| **BUG-028** | 客户端在开机自动启动后，无法正常自动刷新订阅节点（导致无可用节点或节点过期），代理功能瘫痪，必须人工手动点击更新。 | v1.1.4 | **已解决，待确认**。<br>**设计要求**：开机自启动客户端时，如果因为网络尚未加载成功而导致第一次订阅更新刷新失败，不应跳过更新，而应延迟一定时间后进行重试，直至网络通畅并成功刷新。而且，当本地订阅配置文件中缺失 update_interval 时，定时器自动兜底采用 1440 分钟（24 小时）默认更新间隔以注册和运行任务。<br>**代码状态**：已在 Rust 后端 `timer.rs` 中引入默认 1440 分钟更新间隔 of 兜底，并支持开机断网时的 2 分钟重试逻辑，代码已编写完成。 |

| **BUG-029** | 当前活跃出口节点状态栏的元素排列顺序不合理，视觉阅读逻辑混乱。 | v1.1.4 | **已解决，已确认**。<br>**设计要求**：活跃节点状态栏重排为：Label ➜ 协议 ➜ 节点名称 ➜ 延迟 ➜ IP:Port 的线性排列。当窗口变窄缩小时，依次自适应隐藏次要信息，防止文本换行与错乱。<br>**代码状态**：已重排前端 JSX 并设置 Flex 流式布局，利用 CSS `@media` 断点控制不同宽度下分别隐藏 IP:Port、协议类型及 Label 等元素，用户验证通过。 |

| **BUG-030** | 程序初次启动或重载时，即使配置文件默认开启了“系统代理”，流量接管模式的 UI 滑块依然显示为“手动模式”。 | v1.1.4 | **已解决，已确认**。<br>**设计要求**：客户端初次启动或核心重载时，流量接管状态（如系统代理与手动模式）的滑块显示必须 100% 准确同步实际状态，不允许出现实际是系统代理接管但滑块显示为手动模式的时序不一致问题。<br>**代码状态**：已重构滑块状态的驱动源，不再依赖临时渲染缓存，而是直接订阅后端的配置状态字段（`verge.enable_system_proxy` 和 `verge.enable_tun_mode`），用户验证通过。 |

| **BUG-031** | 在开启系统代理时修改 Mixed Port，保存后流量接管模式滑块会自动跳回手动模式，且系统代理端口未同步导致断网。 | v1.1.4 | **已解决，已确认**。<br>**设计要求**：开启系统代理状态下，当用户在侧边栏修改 Mixed Port 并保存时，流量接管滑块不可异常复位到手动，且必须立即将新的端口覆盖同步写入 Windows 系统代理配置的注册表中，防止断网。<br>**代码状态**：后端在 `mixed_port`被写入且保存时，同步下发 `UpdateFlags::SYS_PROXY` 触发器，强制 Windows 系统代理更新并绑定滑块驱动，用户验证通过。 |

| **BUG-032** | 在不同流量接管模式（如系统代理与 TUN 模式）之间切换时，滑块会先跳回“手动模式”再滑至目标模式，造成视觉闪烁和困惑。 | v1.1.4 | **已解决，已确认**。<br>**设计要求**：切换模式（如系统代理切 TUN 或反之）时，滑块切换应保持平滑过渡，不应出现先滑到“手动”再闪烁滑到目标模式的现象。对需要 UAC 提权的 TUN 模式，应在安装完网卡服务前保持滑块状态，并在完成后一步到位。<br>**代码状态**：重构前端 `handleTakeoverModeChange`，合并多次配置下发为单次原子 PATCH 动作。针对 TUN 提权安装，引入过渡等待状态，网卡服务就绪前滑块锁定不乱动，用户验证通过。 |

| **BUG-033** | 导入 3 个或更多订阅链接时，订阅列表没有滚动条，导致用户无法察觉可滚动的剩余配置项。 | v1.1.4 | **已解决，已确认**。<br>**设计要求**：导入大于3个订阅时必须出现垂直滚动条。全局滚动条要统一为 6px 宽度的 3D 拟物凹凸质感窄轨滚动条，而对于主页节点列表和设置页面表格等纯表格，为了极致拟物视觉，应通过局部 CSS 将其隐藏。<br>**代码状态**：全局注入了窄 3D 滚动条样式，且在 `ProxyGroups` 和 `ConnectionTable` 组件上利用 CSS 局部选择器进行了标准滚动条的精细隐藏，用户验证通过。 |

| **BUG-034** | 激活新导入的订阅或切换不同订阅时，系统不会自动进行首次延迟测速、自动排序节点并自动挑选最快节点，导致用户必须手动操作，且无序的列表视觉体验极差。 | v2.0.1 | **已解决，已确认**。<br>**设计要求**：1. 自动触发首次测速、节点按延迟自动升序排列并绑定最快节点，具备循环保护机制。<br>2. 测速与选择机制：开始测速后以 500ms 间隔轮询。在 30 秒内，若扫描到至少 5 个可用节点，立刻从中挑选最快的选中切换；若 30 秒到 60 秒之间，只要有至少 1 个可用节点就立刻选中；若 30到达时有 1-4 个可用节点也立刻从中选最快的切换；若 60 秒仍无可用节点，则弹出大字号报错信息“所有线路都繁忙，请耐心等待”提示用户。<br>**代码状态**：在 v2.0.1 中，自动测速和自动挑选最快节点优化为仅对主页表格过滤框当前展示/过滤出的节点集合执行，避免过滤掉的非目标国家或不可用区域 of 节点被自动测速和挑选选中，确保特定代理策略正常运作。状态已确认解决。 |

| **BUG-035** | 【本轮衍生】在 v1.1.4 引入置顶图钉后，当前活跃出口状态条与图钉按钮发生遮挡，且高度偏高、无法自适应定位和右对齐。 | v1.1.4 | **已解决，已确认**。<br>**设计要求**：状态条高度退回并与图钉/设置齿轮容器高度（28px）保持一致。取消右侧硬编码的 margin 避让距离，在 Upper Pane 顶部将状态条与两个悬浮按钮放置在同一个 Flexbox 行容器中，实现自动定位和右对齐。设置齿轮图标缩小至 20px 保证对称性。<br>**代码状态**：已在前端 `_layout.tsx` 中取消了原本顶层的绝对定位，在 `ActiveNodeStatusCard` 外层采用 Flexbox 包裹并统一设定高度为 28px，取消了原先硬编码避让的 absolute 定位，前端重新编译测试成功。 |

| **BUG-036** | 设置页展开后遮挡了右上角关闭按钮，导致用户无法关闭设置窗口而被锁死。 | v1.1.6 | **已解决，已确认**。<br>**设计要求**：恢复右上角设置齿轮与关闭按钮二合一动态切换的设计（打开时显示 Close 叉号，关闭时显示 Settings 齿轮），实现同物理坐标的顺畅进出，并调整顶部容器 z-index 为 110 防止被遮挡。<br>**代码状态**：已还原二合一动态按钮并调整 header 层级，用户已在 v1.1.6 验证通过。 |

| **BUG-037** | 窗口始终置顶功能仅保留顶部栏图钉快捷按钮，设定页上的“窗口始终置顶”设置项与 Switch 开关需彻底删除，且在设定页展开时隐藏图钉按钮。 | v1.1.7 | **已解决，已确认**。<br>**设计要求**：从设置面板（Drawer）的基础设置列表中彻底移除“窗口始终置顶”项与 Switch 开关，保留前端的 `alwaysOnTop` 置顶状态逻辑与后端 Tauri 的窗口置顶机制。当设定页展开时（`drawerOpen === true`），同时隐藏顶部栏的置顶图钉快捷按钮与当前出口节点状态卡片。<br>**代码状态**：已移除设置页的 Switch 并隐藏相关组件，前端编译验证通过。 |

| **BUG-024** | Windows 标题栏数学粗体字符在部分 Windows 系统下显示为方块或乱码。 | v1.1.2 | 废除了 Unicode 数学符号映射转换，将标题栏文字恢复为标准的 ASCII 字符（例如 `"Clash Mini Ver.1.1.1"`），保证英文字符在所有 Windows 版本上正常呈现。 |

| **BUG-025** | 滑块选项文本只有在被选中时才是粗体，与普通按钮全部加粗不统一。 | v1.1.2 | 在 `_layout.tsx` 中将三个三选一滑块的所有文本选项字重（fontWeight）常驻设置为 `'bold'`，无论是否处于选中状态均保持一致加粗。 |

| **BUG-026** | 界面控制尺寸显得过于偏大，尤其是主按钮、双滑动条和三选一滑块。 | v1.1.2 | 执行了控件尺寸微缩精细调整：主按钮（导入订阅和运行日志）统一调整至 `24px`；双滑动条轨道高度与滑珠直径缩减至 `10px`；三个三选一滑块高度统一调至 `26px`；路径控制的二选一滑块和断开/清空按钮统一调至 `24px`；小开关保持 `14px` 不变，实现极致紧凑拟物效果。 |

| **BUG-018** | 底部指标卡片整体偏大，且在折行及小屏状态下字体比例稍显失调。 | v1.1.2 | 将 Metrics Row 长条高度缩减为 26px，卡片高缩减为 22px，图标大小调至 16px，数字字号设为 15px，单位字号设为 10px，并将“上传:”、“总量:”、“下载:”、“总量:”这 8 个汉字标签字号统一调整为紧凑的 11px，其余 padding 同步调小，完美对齐紧凑精致排版。 |

| **BUG-016** | 代理模式在非规则模式（如全局、直连）下隐藏表头导致定位、延迟排序按钮丢失 | v1.1.1 | 在 `enforce_mini_agreements` 规则重构流程中，在末尾 `MATCH` 规则之前注入局域网直连规则及国内流量（CN）直连规则，从而打破 DNS/DoH 解析循环依赖死锁，解决联网及局域网互访故障。 |

| **BUG-015** | 配置文件增强与激活逻辑在 `_layout` 的 `useEffect` 中与未缓存 of `activateSelected` 发生联动，导致启动和重载后陷入无限循环重新加载并弹窗报错“配置校验失败”。 | v1.0.9 | 再次从 `_layout.tsx` 的 `useEffect` 依赖项中移除 `activateSelected`，仅保留 `currentProfileUid`，并在行尾添加 eslint 禁用注释，彻底斩断无限更新环。 |

| **BUG-014** | 在全局代理（Global）模式下，用户选择节点无法控制流量出口（节点选择脱节）或启动/重载后断网的问题。 | v1.0.4 | 在启动、配置激活和模式切换时，强制将内核 GLOBAL 组绑定指向唯一代理组 PROXY，确保全局出口与用户界面选中的节点保持 100% 同步一致。 |

| **BUG-002** | 默认开启 IPv6 且缺乏界面控制，可能导致某些网络环境下 DNS 泄露或代理分流异常。 | v1.0.3 | 在 `clash.rs` 默认模板中将 `ipv6` 设为 `false`；在 `tun.rs` 逻辑中，当 `ipv6` 字段未定义时降级为 `false`。 |

| **BUG-003** | DNS 覆写默认未开启且缺乏自动优化的评估与改造待办，导致国内直连和防泄露体验不佳。 | v1.0.3 | 将 `enable_dns_settings` 默认值设为 `true`；初始化时如果 `dns_config.yaml` 不存在，则自动写入一份深度优化的 DNS 覆写配置。 |

| **BUG-011** | 默认未开启代理守护（Proxy Guard）导致系统代理易被篡改或静默失效。 | v1.0.3 | 将 `enable_proxy_guard` 默认值设为 `true`，以在后台自动校验并修复系统代理。 |

| **BUG-012** | 默认未开启自动轻量化模式（Memory Optimization）导致后台挂机占用内存较高。 | v1.0.3 | 将 `enable_auto_light_weight_mode` 默认值设为 `true`，自动轻量化延迟 `auto_light_weight_minutes` 默认缩短为 `5` 分钟，在最小化挂机时自动销毁 WebView 进程，节省内存。 |

| **BUG-013** | 自动检查更新选项开启（auto_check_update）引发的冗余后台行为。 | v1.0.3 | 将 `auto_check_update` 默认值设为 `false`，彻底停用冗余的自动检查更新机制。 |

| **BUG-001** | 开机自启非管理员权限下修改后台静默失败且前端 Switch 状态脱节 | v1.0.2 | 前端 PATCH 失败时执行回滚回刷；后端新增 `create_task_elevated` and `remove_task_elevated`，在非管理员权限下注册或删除任务时，通过 Windows `runas` 完美唤起 UAC 提权并完成操作。 |

| **BUG-004** | 代理模式在非规则模式（如全局、直连）下隐藏表头导致定位、延迟排序按钮丢失 | v0.2.0 | 修改前端组件，使表头操作行在「全局代理」和「全局直连」下保持常驻显示。 |

| **BUG-005** | 表格最外侧左、右竖边框线非 5px double 且左侧表格线缩进空缺十几个像素 | v0.2.0 | 修正表格组件左右纵向边框为双线；调整左边距偏移实现完美闭合。 |

| **BUG-006** | 界面清除毛玻璃不彻底，部分 Aero 样式变量残留导致滚动漏光重叠 | v0.2.0 | 完全移除了全局 SCSS and React 组件中关于 rgba 半透明、Mica、Acrylic 以及 Aero 变量的引用，全部采用 100% 实体纯色和 MUI 语义化颜色。 |

| **BUG-007** | 订阅管理中偶现没有名字的空行订阅项，重启后仍反复出现 | v0.2.0 | 前端渲染解析时强制对空名称的订阅项进行过滤剔除。 |

| **BUG-008** | 设置页面右上角设置齿轮按钮与底部的节点名称/延迟重合且无法点击 | v0.2.0 | 重新调整了齿轮按钮的固定定位布局（36x36），并为下方活跃出口节点栏右侧增加 44px 避让间距，防止视觉重合。 |

| **BUG-009** | 启动时提示有 Clash Verge 新版本并引导升级从而覆盖客户端的问题 | v0.0.1 | 彻底废除并禁用了检查官方版本更新的机制与升级 API 接口。 |

| **BUG-010** | 便携版绿色压缩包在 Release 时丢失或包含旧版本二进制的问题 | v0.3.2 | 修改 `portable.mjs`，确保在 Tauri 生产编译（Exit Code 0）完全结束后方运行打包逻辑。 |



---



## 📌 待优化与设计微调 (UI/UX Enhancements)



| 优化编号 | 改进项与现象描述 | 目标版本 | 目前状态与设计方案 |
