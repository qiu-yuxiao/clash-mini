# Clash Mini 开发设计书

> 本文档是 Clash Mini 项目的权威设计规范与开发指南。所有代码修改必须符合本文档中的规则。
> 本文档只描述**设计意图（要做什么、为什么）**，不规定具体函数名、文件名或变量名——实现细节见配套文档 `docs/implementation-notes.md`，变更流水账见 `docs/audit-log.md`。
> 维护备注：窗口缩放采用自定义指针事件驱动、以动画帧节流调用尺寸/位置设置接口的方案，取代了会触发 Windows 模态缩放死锁的系统 resize API；轻量模式唤醒后窗口尺寸恢复采用「进入时存盘、唤醒时读回套用」的方案。

## 目录

- [🏗️ 一、项目定位与架构](#一项目定位与架构)
- [🎨 二、界面与交互](#二界面与交互)
- [⚙️ 三、内核与运行时](#三内核与运行时)
- [💤 四、轻量模式](#四轻量模式)
- [📡 五、后台监控与自愈](#五后台监控与自愈)
- [🛡️ 六、安全与健壮性](#六安全与健壮性)
- [📊 七、数据流与 API](#七数据流与-API)
- [🔧 八、工程规范](#八工程规范)
- [附录 A: Bug ID 映射表](#附录-a-bug-id-映射表)

---

## 🏗️ 一、项目定位与架构

### 1.1 项目定位

为了保证 Clash Mini 与原版 Clash Verge 在同一台 Windows 机器上**完美并存、互不干扰、流畅运行**，必须在前端、后端与打包配置中实施彻底的命名、端口及数据隔离。

1. **开发边界**：允许根据项目需求对后端代码做必要的重构与优化（例如窗口尺寸限定、UAC 提权自启动、本地配置与代理地址解析、定时更新重试等），但修改时必须遵守避坑红线，不破坏系统底层服务稳定，并始终维护与原版的多实例共存物理边界。
2. **显示名字与内部代号**：前端所有面向用户的界面，软件名称必须精确显示为 **`Clash Mini`**；程序打包名也必须使用精确的 `"Clash Mini"`。
3. **数据与配置文件隔离**：Clash Mini 与原版的配置相互独立，配置文件、数据库与订阅列表在用户本地独立目录下加载（沿用原版后端逻辑以防破坏稳定性）。
4. **网络端口避让**：默认 Mixed 混合代理端口设为 `10801`，Controller API 默认端口设为 `9098`（支持启动冲突自适应规避），彻底避让原版默认端口，允许两个客户端同时在线。
5. **系统服务共享机制**：保持系统服务注册名不变。新用户启动 TUN 模式时自动触发 UAC 安装该服务；若原版已安装，通过 SCM 共享管道接管服务运行独立的内核配置，实现无缝切换。
   - **启动模式避让规范（TUN-Service 隔离）**：为消除与原版的 SCM 服务抢占，保护用户网络代理不被打断，**只有当 TUN 模式开启时，才允许启动服务模式接管**；TUN 关闭时无论服务是否就绪，主程序启动及内核重载均必须强制进入旁路模式，在本地独立启动内核进程，与原版并存互不干扰。
   - **服务管理器稳定态架构**：服务管理器必须采用防并发的状态机结构，并保留自定义内核的本地路径检测机制，确保极端稳定性与向后兼容。
6. **多语言支持与翻译规范**：恢复并支持原版所有 13 种语言。严格遵守**「英文保留原则」**：中文版使用英文的词汇（如 `Mixed Port`、`Allow LAN`，以及各风格双滑块标签 `Depth`/`Vibrancy` 等）在其他语言版本中必须保持英文。
   - **三选一分段按钮宽度自适应**：240px 宽设置侧栏内防止文本撑爆或折行——中文显完整词、日文显极简假名/汉字、韩文显极简韩文、西文统一 3 字母大写工业规格（`MAN`/`SYS`/`TUN`、`DIR`/`RUL`/`PRX`、`SYS`/`LIGHT`/`DARK`），西文缩写按钮绑定 Tooltip 显示完整名。
   - **悬浮提示 / 底部控件与换肤适配**：帮助按钮、语言选择器等控件须随六种皮肤动态切换，样式不被默认样式覆盖。
   - **语言切换死锁回滚防范**：为避免手动切换语言触发异步写入数据库导致旧值反向覆写回滚，必须追踪最后一次同步的语言配置，仅当数据库返回值与保存值不一致时才执行同步，切断死锁回滚环路。
7. **禁用自动更新**：彻底禁用检测及提示原版 Clash Verge 新版本的更新机制，防止升级覆盖损坏项目独立性。
8. **单实例检测端口隔离**：单实例检测端口在 Release 模式设为 `33335`、Dev 模式设为 `33336`，禁止使用与原版相同的端口，杜绝单实例机制互锁导致的冲突和闪退。
9. **内核与退出孤儿进程物理隔离**：内核二进制名称前增加 `mini-` 前缀，在磁盘目录与操作系统进程级别建立独立命名空间。程序退出时必须扫描并强制杀灭所有含 `mini-` 前缀的残留子进程，且绝不会误伤原版进程。
10. **单实例 IPC 静默通知规范**：第二实例启动时若检测到端口已被占用，必须向已有实例发送唤醒通知（唤醒窗口及处理 deep-link）；通知成功送达后第二实例必须静默退出，不得弹错误框。错误框仅在通知失败（端口占用但请求超时/拒绝）时展示。
11. **单点测速排序与活跃节点延迟响应规范**：
    - 延迟升序下，单点测速完成后必须驱动列表重排。
    - **排序优先级**：正常延迟 → 超时/错误/测试中 → error；`-2`（测试中）等哨兵值不得排到 timeout 后面。
    - **活跃节点响应**：头部卡片延迟值必须通过响应式订阅更新，不依赖全量代理数据变化驱动重算。

---

### 1.2 壳进程内存占用基准

壳进程物理内存约 35MB 为**正确行为**，原因：
- 旧版进入轻量模式时调用工作集暴力修剪 API，导致 WebView2 恢复时大量 page fault 引发挂起/死锁，已彻底删除，交由系统自行管理。
- 旧版在窗口隐藏时强制压制 WebView2 内存级别，同样是死锁诱因，已废弃删除，运行于默认级别。
- 该数值在轻量模式（销毁主窗口）下会显著回落。内核进程内存已通过 Go runtime 环境变量（`GOMEMLIMIT=96MiB` / `GOGC=50` / `GOMAXPROCS=2` / `geodata-loader=memconservative`）约束，不再不必要膨胀。

### 1.3 版权与著作权
- 整体继续遵守 GPL-3.0-only。
- 开发者仅对 2026 年起的二次开发部分主张著作权。
- 保留并尊重原项目版权与署名，界面版权页脚展示 `© 2026 秋雨潇潇 (修改部分)` 及联系邮箱。

### 1.4 管理员模式与内核进程管理
- **管理员权限跳过服务等待**：以 Administrator 运行时已具最高网络控制权，启动时必须先检查是否管理员，若是则跳过服务等待轮询。
- **同步防阻塞**：管理员运行时立即返回跳过异步服务重试，确保内核发起配置重载前已就绪，消除双进程内核残留泄漏。
- **非管理员 + TUN 启动回退**：系统服务未安装则自动 UAC 提权安装；失败/超时则将运行模式自动重置为系统代理模式（非 TUN）并保存，用旁路模式本地启动内核，保证开箱即用。
- **废除运行期自动版本比对与重装**：运行时禁止对系统服务做版本比对与静默提权重装；服务已安装但停止时仅允许启动。
- **后台代理恢复与选点触发（无前台窗口）**：前台无窗口状态（开机静默启动、转入轻量模式等）下的测速选优及前次节点恢复必须完全由后端接管：进入轻量模式时读取当前 Profile 的 `selected` 历史记录并对内核策略组做节点覆盖还原；还原后异步触发全节点延迟测试，自动切换为可用且最快节点；若因开机断网首次测速失败，后台探测到网络恢复时必须立即重新执行一次自愈优选。

---

## 🎨 二、界面与交互

### 2.1 3D 拟物美学最高准则
界面全面落实 3D 实体拟物与光影立体美学，与扁平化风格完全隔离。支持立体磨砂（Depth）与色彩霓虹（Vibrancy）双控制变量动态调节。

### 2.2 布局框架
窗口内容采用**「遮挡式」布局**而非「压缩式」布局：窗口缩放时元素不改变大小、不重新排列、不被挤压变形；窗口像取景框，宽则露出多、窄则只露最关键内容，其余被挡在框外。

整窗只有两处随窗口宽度切换结构：① 代理节点表格宽窗 3 列、窄窗（≤285px）1 列；② 当前活跃出口节点栏宽窗 4 项、窄窗 2 项。其余元素不随尺寸变形态。宽窄临界点（窗口宽度约 285px）必须在软件各处统一，否则会出现错乱。

- 设置抽屉内的右侧连接/路由表同样遵循整块显隐：窄窗（285）下整块不挂载、不渲染、不订阅，宽窗（>285）才挂载同步，临界点严格统一。
- 主界面必须紧凑扁平精致质感，拒绝粗大臃肿现代 UI。

**双层常驻布局**：
- 界面不设侧边栏/多页面导航，主视口为垂直 Flexbox 布局。自定义标题栏常驻（高度 30px），视口高度相应扣减；隐藏标题栏模式下视口为全高。
- 上半层展示当前活跃节点及节点/延迟列表；极窄模式（≤285px）下高度自适应。
- 下半层展示全宽流量监控折线图与指标数据，普通模式固定高度 `135px`、极窄模式 `100px`，折线图物理高度随容器缩减并切换为 Compact 小图。
- 底部指标行 4 个指标卡片（上传/下载速度、上传/下载总量）高度固定、微凸起立体按钮感，极窄模式下垂直堆叠。

**滑出设置面板**：点击齿轮图标往左下方滑出，刚好覆盖上半层，下半层流量图仍可见高频更新。设置页边框 `4px double` 双实线。设置页内左右分栏：左侧固定 240px 摆放订阅区/模式选择/策略倾向/基础设置/主题设置；右侧自适应放置路径控制。

**窗口装饰与标题栏**：
- Windows/Linux 启动为 frameless 自定义标题栏（30px，左图标+名+版本号，右最小化/最大化/关闭）；macOS 用原生标题栏。标题栏文字统一标准 ASCII（如 `"Clash Mini Ver.1.1.1"`）避免乱码。
- 标题栏整体为可拖拽区域（除右侧控制按钮）。
- **窗口缩放**：frameless 模式通过 8 方向透明 resize handle（10px 宽、四角 20px）实现边缘拖拽缩放，最大化时自动隐藏。**禁止使用会触发 Windows 模态缩放死锁的系统 resize API**（该 API 在 UI 线程内同步触发 COM 调用，可能永久阻塞导致窗口未响应）；必须采用自定义指针事件驱动、以动画帧节流调用尺寸/位置设置接口的方案，resize 期间设置全局标志跳过尺寸变化回调中的 IPC 调用以防 IPC 洪水。批量测速已整体迁后端，前端测速期间仅持一个 IPC，故 resize 始终可用，无需在测速期间锁窗口。
- **隐藏标题栏模式**：窗口处于最小宽度（285px）且高度 ≤135px 且连续 10 秒无操作，激活隐藏标题栏模式（GlowBorder 接替窗框）；齿轮/图钉按钮仍显示于内容区顶部。左键位移 >5px 可拖拽移动；释放（位移 <5px）或拖宽脱离 285px 恢复标题栏。二者互斥，无垫片补偿。
- **默认与极限尺寸**：启动默认 285×680；最大 640×860；最小 285×135，任何情况下（含拖拽中）不得缩小至最小尺寸以下；最小化/最大化态不生效。
- **Windows 初次启动尺寸精确性**：Tauri 在 Windows 创建无装饰窗口时底层未清除标题栏样式位，首次创建瞬间会按 style 自动补上标题栏，导致 outer 比设定的 logical 多出约 15-22px（实测 300 而非 285）。修复必须在窗口创建成功之后、于主线程主动清除标题栏样式位（保留可调边框位），并直接用 Win32 设置 outer 尺寸（按 DPI 缩放）绕过框架的 inner_size 计算偏差。该修复必须在窗口创建成功分支执行，不能在窗口创建异步任务之前的早期启动事件中执行（彼时窗口句柄尚不可用）。

**自定义内发光边框（GlowBorder）**：仅在隐藏标题栏模式下显示，紧贴内容区外侧（不占内容区空间，窗口尺寸无需为其调整），与 Windows 原生窗框互斥交替。宽度 4px。颜色为琥珀金与电光蓝双极点，在封闭管道内运动（靠近减速、远离自由漂移），速度随网络流量呼吸（无流量 8-10s/圈、正常 4-5s、较高 3s、爆发 2s）。发光亮度双层叠加（呼吸 60% + 心跳 0.9s/次），强度受 Vibrancy 滑杆控制。

**窗口边缘磁吸**：无边框模式下支持边缘拖拽吸附（距边缘 <18px 吸附），150ms 冷却；拖动时预缓存显示器与窗口大小避免高频 IPC。

**当前活跃出口节点一键轮换（FEAT-006）**：点击节点名称强制更换为当前过滤/排序子集内下一个节点。从本地存储读取过滤文本与排序 → 对主组所有节点做去空格、大小写不敏感字面子串模糊匹配与排序得候选子集 → 子集内循环轮换（当前不在子集则切到子集首个）→ 立即更新后台与界面 → 名称上显示「点击轮换」气泡。

### 2.3 字体与字号 / 视觉风格
- 顶栏按钮（置顶/设置）遵守六种皮肤 3D 拟物样式动态换肤，间距压缩。
- 禁用态按钮必须有高对比度样式，避免零反差字形缺陷。
- **禁用窗口透明与虚化**：窗口设为不透明，清除所有虚化滤镜与半透明背景，主背景/面板/菜单/对话框全部 100% 实体纯色不透明。保留经典分割线结构（表格 2px 网格线、中缝 5px double、设置页 4px double），仅剥离表面发光特效。
- **MUI 语义化与 Aero 变量肃清**：移除残馀 Aero 虚化样式，统一使用主题语义化颜色变量。

### 2.4 流量图配色与防挤压
- 上传（琥珀金）/下载（电光蓝）必须用标准十六进制，禁止动态拼接 rgba 字符串（防 Canvas addColorStop 抛错闪退）。
- 仅 Document 隐藏态休眠；窗口可见时尺寸与 CSS 实时同步。
- 图表内边距与坐标轴自适应；底部 4 卡片按下载组在左、上传组在右排布，图标严格在文字前，`whiteSpace: nowrap` 防折行。

### 2.5 输入框与按钮 / 基础设置
- Mixed Port 输入框宽 80px，失焦或回车时自动校验并保存。
- 订阅链接输入框两行布局（多行文本框 + 居中导入按钮）。
- 所有按钮/输入框/选择控件必须符合 3D 拟物美学。
- **Allow LAN 开关**：移除原「通知弹窗」开关（默认锁死 true），替换为 Allow LAN 3D 拨动开关，动态读写内核 `allow-lan` 配置并热更。
- 主题模式三选一（系统/浅色/深色）；双滑动条 Depth/Vibrancy，数据本地持久化并注入顶层 CSS 变量全局联动。

**多套控件皮肤（6 种）**：Trump-3D（默认，金条拟物）、Original（原版致敬）、Modern（扁平现代）、Frosted Glass（磨砂玻璃）、Cyberpunk（暗黑霓虹）、Monochrome（黑白）。换肤选择器单行 Excel 风格横排，所见即所得（选中格渲染该皮肤真实按钮外观）。各皮肤双滑块标签与范围不同（见原设计细节，参数以现存前端为准），本地存储独立字段避免冲突。滑块行单行布局压缩（名称+数值左、滑块右），高度释放由 flex 吸收。中英文字体族共鸣对齐。中缝/设置页/表格双线及 GlowBorder 在任何皮肤下保持现状；GlowBorder 静态设计降渲染负载至 0% 且随皮肤切换。

### 2.6 系统托盘极简交互（BUG-073）
为避开 Windows 高权限下托盘 API 频繁更新导致的 `E_FAIL` 错误，托盘强制极简静态化：
- 仅支持两功能：左键恢复主窗口原状；右键弹仅含「退出」的静态菜单。
- 图标常驻默认静态头像，提示语固定。运行期间**禁止调用任何动态更新图标/提示语/菜单的 API**，杜绝 `E_FAIL`。
- 托盘图标必须托管于应用状态管理器，生命周期与程序完全对齐。

### 2.7 主页节点列表（三列网格）
- 每行 3 节点（极窄 1 节点），单节点高 20px。裁剪「协议/类型」列，仅留名称+延迟。
- 网格线 2px solid；列间 5px double 双线；延迟列宽 65px 居中。
- **节点点击行为**：整行作为统一点击目标，仅执行「设为当前活跃节点」；不提供单点测速入口。批量测速唯一入口为表头闪电按钮；单点测速仅活跃节点卡片提供。
- **表头固定行**：规则/全局代理/全局直连模式均必须显示；过滤框常驻可见（漏斗图标已移除）；统一精简为闪电测速+排序+过滤框。
- **最窄窗口（≤285px）**：退化为单列，每节点横排 协议/名称/延迟。

### 2.8 极光背景与 3D Bevel 视觉层 / Hover 动效
- 极光动态背景（三径向渐变中心）通过 Vibrancy 变量缩放。
- 毛玻璃材质与 3D 浮雕光影由 Depth 变量联动（backdrop-filter 磨砂、内外发光阴影、霓虹背光）。
- 节点单元格 Hover 物理回弹抬升 + 扫光；底栏数据卡片 Hover 微抬升增强发光。

### 2.9 帮助按钮（FEAT-005）
带圈问号图标，绝对定位固定于上半层底边，留空区保留给后续设计，100% 随皮肤切换，窄高时响应式隐藏，点击在默认浏览器打开项目主页。

### 2.10 全局窄滚动条
除特定 Excel 风格表格外，恢复显示窄 3D 滚动条；节点列表与连接表格必须保持滚动条不可见。移除特定容器的滚动条占位，充分利用窗口宽度。

### 2.11 窗口操作线程安全（BUG-259）
所有窗口操作（隐藏/显示/最小化/聚焦/置顶/关闭）**必须在 UI 主线程执行**，禁止异步线程直接调用（共享状态竞态导致崩溃）。统一将窗口操作调度到主线程封装，外部禁止直接对窗口对象调用变更类操作；只读查询可在任意线程。

---

## ⚙️ 三、内核与运行时

### 3.1 启动流程
设计目标：用户点击启动/停止/重启立即响应、稳定，不因误操作或状态混乱卡死。内核操作互不阻塞；重复点击无副作用；关闭过程中不启动；启动失败可重试（自动清理残留进程）；Windows+TUN 下服务未就绪则后台等待衔接；配置修改与内核操作互不冲突。

### 3.2 运行模式判定
启动时根据 TUN 配置决定服务模式或旁路模式；TUN 未启用直接旁路，不查服务状态；Windows 下需等待服务 IPC 就绪。

### 3.3 内核更新
- **Help 按钮上弹菜单**：改造为向上弹出气泡菜单（GitHub 主页 / 检查软件更新 / 检查内核更新），与 6 种皮肤一致联动。
- **客户端更新静默后台链**：更新公钥与服务端点指向 Clash Mini 自身分支配置与 Release 下载路径，杜绝向原版检查覆盖；app 启动生命周期挂载后台静默检测与缓存下载。
- **内核动态路径与平滑更新**：优先检索用户可写目录下自定义内核，否则用包内嵌入兜底；通过官方 API 获取最新版本下载解压替换可写目录内核，若系统服务模式开启则先卸旧服务再重装启动防写入锁死。
- **后端自动代理多重降级**：所有访问 GitHub API/下载内核的逻辑严禁使用不带代理的原生 HTTP 客户端，必须托管于网络管理器，依次降级 Localhost 代理 → 系统代理 → 直连，最大限度保证连通。
- **前端内核版本格式化**：必须用通用格式化助手剥离多重 `v` 前缀、统一 `Ver.` 开头、删除冗余「Mihomo」字样。
- **下载超时与全生命周期 Fallback**：响应体读取设 20 秒超时上限打破无限阻塞；将建连与读流合并为尝试单元，依次 Localhost→System→None 顺序回退（任一环节错误自动捕获回退下一代理）。TCP 连接超时 10 秒加速坏代理探测；整包读取大超时 300 秒防大文件中断。
- **相同版本阻断与静默提示**：更新检查前必须版本前置比对，相同版本严禁弹下载框或启后台安装，统一用 info 气泡静默提示。

### 3.4 Mihomo 插件 API
插件 JS 库调用后端接口时，传参对象键名在序列化时必须正确匹配 Rust 接口字段名，防止参数拼写错误导致接口失败、阻塞前端渲染。

### 3.5 端口冲突处理
- **更换默认密码**：废弃所有硬编码默认密码，外部控制密钥设为固定值，保护控制接口不被未知本地程序窥探。
- **控制端口启动冲突自动规避**：启动阶段动态检测默认控制端口可用性；若端口无法 bind，发验证请求，带密钥成功则判定为自身残留进程、直接复用；失败则判定被第三方抢占，自动向后递增探测首个空闲端口并更新配置。整个过程无感完成。

### 3.6 Geodata 加载策略
`geodata-loader` 默认 `memconservative`，按需加载 GeoIP/GeoSite（仅路由匹配需要时才载入），降低约 5-10MB 内存峰值，性能影响可忽略。

### 3.7 Go Runtime 环境变量
启动内核子进程注入 `GOMEMLIMIT=96MiB` / `GOGC=50` / `GOMAXPROCS=2`（均支持系统环境变量覆盖），将内核堆控制在约 96MiB 内、限制线程数，契合常驻托盘应用低资源定位而非极限吞吐。

---

## 💤 四、轻量模式

### 4.1 进入与退出状态机
点击关闭按钮后立即进入轻量模式（销毁主窗口释放内存/GPU），而非仅隐藏等定时器（旧定时器机制已全部移除）。
- 关闭按钮：先隐藏窗口，立即进入轻量模式同步销毁主窗口。
- macOS：隐藏操作替换为直接进入轻量模式，跨平台一致。
- 配置项 `enable_auto_light_weight_mode` 字段保留但无运行时行为；自动轻量启动由专用启动判断直接读取静默启动配置，不再经过配置热更新路径。
- 退出轻量时窗口可用即视为成功，不触发状态机回滚；关闭自动轻量时仅隐藏窗口仍同步托盘菜单；退出时须重置失败计数和重试模式，防止轻量期间状态影响正常模式。

### 4.2 连接清理与订阅熔断
进入轻量模式后**不断开已建立的网络连接**（用户铁律：切节点/轻量进入均不断链），仅清理后端开销：
- **主动熔断前端 WebSocket 常驻订阅**：立即断开并清理所有前端 WS 实时流通道（流量/日志/连接），截断内核到前端数据分发链路，使 CPU/内存降为零。
- **UI 生命周期自动恢复**：退出轻量时无需手动重建 WS——主窗口重建挂载时前端组件自身挂载逻辑会自动发送新 WS 建立指令，唤醒时数据流无感瞬连。

### 4.3 后台监测周期恒定
后台健康监测线程以**恒定 15 秒**周期运行，不随前台 / 后台 / 轻量状态改变：
- **恒定周期的设计依据**：Clash Mini 是代理程序，节点健康与「代理程序在前台工作，还是关掉前台只在后台（轻量模式）工作」**根本无关**——代理路由与流量始终由内核承载，窗口开或关对内核监测需求没有区别。因此轻量模式不需要、也不应放慢监测周期；恒定 15 秒既是及时性（节点假死约 15 秒可发现）与开销（每轮仅对测量组做一次内核级拨测）的合理折中，也是当前正确且唯一的设计。
- **重试阶段加速**：一旦判定进入重试（节点异常），周期统一收紧至 **3 秒**，确保连续失败快速自愈（详见 §5.1）。
- **瞬时唤醒即时探测**：监测线程须监听轻量唤醒通知；退出轻量恢复主窗口后须显式发唤醒信号，使线程立即跳出当前休眠、无延迟发起一次探测并回归 15 秒常规规格，实现无缝高频监控。

### 4.4 窗口唤醒刷新
解决唤醒后节点列表延迟显示为 `-` 的问题：
- **解决双挂载/竞态取消导致初始化中断**：布局组件挂载副作用清理函数中，若因重渲染导致清理必须重置已处理标记，确保下次挂载能正确拉起测速流程，防止首次启动测速被永久拦截。
- **自适应窗口可见度唤醒测速**：顶层事件监听拦截窗口聚焦与文档可见度变化；文档变为可见且启动过程已完成、且距上次全节点测速超 30 秒（防切出应用重复刷新）时，立即委托后端对 PROXY 组执行静默全节点测速填充（严禁切换用户当前节点），结果经后端事件回传前端刷新。该测速非阻塞，仅静默恢复延迟缓存。

---

## 📡 五、后台监控与自愈

### 5.1 节点健康监测与自动选点
为保证无人值守/重启/轻量/前端关闭等状态下 24/7 自动健康监测、失效自愈及过滤条件持久化记忆：
- **前端输入决定，后端记忆执行**：前端搜索/正则过滤/测速/手动选点状态须通过后端接口实时传递并持久化至后端专门状态配置文件；后端守护线程运行和自动选点时须读取并严格遵循该持久化过滤条件，开关机或前端关闭后过滤条件始终在后台生效。
- **Local Socket 通信与就绪检查**：后台监测与自动选优的所有内核通信必须通过内核内置本地套接字/管道客户端完成，**绝对禁止**用 raw TCP 请求，以防 `external_controller` 禁用时端口拒绝。监测线程须先等本地核心 API 就绪、确认节点列表已加载完成再判定，防配置切换/内核加载期间误报。
- **测量统一委托内核（无应用侧并发池）**：后端自动选点通过单次 `delay_group` 调用，委托 mihomo 内核的 `PROXY__METRICS`（url-test 类型）统一测量全量节点延迟；并发由内核内部调度，应用侧不维护 worker 池（旧「并发上限 32」的 worker 池已随 v2.6.6 测量层根因修复删除——per-node 自测在 TUN 下会回环 timeout，故改为内核组统一测量）。
- **精确防抖自愈时间参数**：
  - 检测周期：常规 **15 秒**；重试阶段 **3 秒**。
  - 防抖自愈触发：连续 **2 次**检测失败才触发批量选点自愈。
  - 测速超时：统一 **2000ms**，有效延迟须 ≥30ms 且 <2000ms；<30ms 判为假/广告节点剔除。
  - 探针超时 == 判死阈值 == 2000ms，前后端统一；超过 2000ms 一律按不可用，UI 显示 Error/Timeout。
  - 每次判定前必须对测量组做内核级即时拨测，消除依赖固定 15 秒间隔导致的「节点假死至多延迟一轮才发现」盲区。
- **自愈选点失败冷却**：失败或结果为空（所有节点不可达）进入 **60 秒**冷却，冷却期内不重复触发；成功则无冷却。因系统繁忙导致的选点冲突不算失败，不施加冷却。
- **连续自愈失败系统警报**：连续 **5 次**全部失败弹 Windows 提示框告警。
- **API 异常不可静默忽略**：内核 API 不可达返回错误时日志须为 warn（非 debug）并确保可见；维护连续异常计数器，连续 3 次异常须触发一次自愈尝试恢复。旧代码把错误当「内核重载中」仅输出 debug 且不计数不自愈，导致内核卡死时监测彻底失效、轻量模式断流十余分钟。
- **轻量模式唤醒不得触发内核 force 重置**：轻量进入/退出是前端 UI 状态切换（窗口销毁/重建），内核须继续服务，不得重置。进入/退出不得调用任何配置 reload；只允许熔断前后端 WS 订阅。前端 profile 增强必须区分「profile 切换」与「窗口重建」——因 React 重挂载时 useRef 重置无法区分，必须用 localStorage 持久化「已 enhance 的 profile uid」（永不清除，enhance 成功才写入），检测到相同则跳过 enhance 仅恢复节点选择 + 测速刷新，避免误触发内核全栈重建导致 8 秒代理中断。
- **网络连接防死锁安全**：所有后端监控客户端及 Local Socket 请求必须配置 **3 秒**显式超时，防核心挂起致异步线程无限阻塞（通用测速 API 不受此 3 秒硬限，而用动态超时以允许高超时设置或排队测速正常返回）。
- **无竞态前端缓存同步**：前端状态同步钩子须在挂载时一次性载入全配置过滤词字典，仅内存状态修改时写本地持久化与后端文件，禁止在订阅切换的副作用中做异步网络请求，消除写入覆盖风险。
- **后端自愈模糊字面匹配**：匹配过滤词时任何情况均用大小写不敏感、去首尾空格的纯字面子串模糊匹配，杜绝正则逻辑漂移与安全隐患。
- **后台自动选点静默机制**：除错误外保持完全静默，避免高频弹通知。
- **网络状态感知与在线恢复**：定期用测速网址域名做 DNS 探测（不依赖第三方）；在线每 **60 秒**、离线每 **5 秒**探测；由离线转在线时立即重置失败计数与重试模式并检测当前节点健康，不可用则立即触发自愈。冷启动期检测到未连通须等待，不计入失败、不施加冷却。

### 5.2 自愈选点失败冷却
失败或结果为空进入 60 秒冷却；成功无冷却；系统繁忙冲突不算失败不冷却。（详见 5.1）

### 5.3 资源占用控制
为降低后台挂机及日常 CPU 占用，在通信/渲染/生命周期层面遵循零泄漏防线：
- **隐藏与最小化通信静默**：流量/连接数据 Hook 在窗口不可见时其 WS 订阅必须彻底断连、停止解析，不挂载订阅。
- **连接面板双模切换与免合并轻量化（数据/渲染统一开关）**：设置抽屉内右侧连接/路由表与它的 WS 订阅共用同一窗口宽度开关——大窗挂载并高频实时观测（16ms 节流全同步 + 差量计算），窄窗（285）完全不挂载不渲染、订阅关闭，数据与 DOM 同时开同时关；抽屉关闭或窄窗时连接管理进入完全静默（断 WS、停请求、冻结最后已知状态、不再降频轮询）。这是经确认的设计决策。
- **设置抽屉小窗口极简降级（数据订阅与 DOM 挂载同一开关）**：小窗口尺寸（宽≤285 且 高≤135）下打开设置抽屉时设置内容块整体不挂载，仅留底部流量图与关闭叉；服务于设置 UI 的查询 `enabled` 必须共用同一尺寸信号（打开且非小窗），与 DOM 挂载条件同时开同时关，小窗下不触发任何 IPC。
- **本源/附加分层原则**：本源层 = 主窗口基线（活跃节点栏、节点列表与检索/排序首行、底部流量图及常驻数据）永远挂载；附加层 = 设置抽屉独占内容与数据，仅抽屉打开时存在、关闭即释放。凡仅服务于设置抽屉内容的查询 `enabled` 一律挂「打开且非小窗」，关抽屉即释放，不得仅挂「非小窗」（否则关抽屉后仍后台空跑）。
- **日志组件条件渲染与彻底注销**：日志弹窗关闭时直接 React 卸载日志组件，使其 WS 连接在关闭第一时间彻底销毁，杜绝后台堆积合并解析。
- **布局端连接轮询彻底消除**：侧边抽屉连接列表未展开时严禁用 HTTP API 获取连接详情；底部流量总量必须直接解析自流量 WS 推送流内置字段，零 IPC 轮询。
- **GlowBorder 静态化**：外框完全静态自适应皮肤设计，消除动画循环，CPU/GPU 负载降至 0%。
- **流量采样折线图重绘节流**：采样快照周期从 200ms 提高到 1000ms，显著减小主线程处理与强制重绘频率。
- **运行时间轮询消除**：去除未使用的运行时间接口轮询。
- **节点组数据可见性管控**：窗口隐藏/最小化时断开代理数据 WS 推送，恢复可见时自动重连，无需定时轮询。

### 5.5 批量测速与自动优选
主界面闪电按钮（per-group，即 F4）触发对**当前界面可见节点子集**的批量并发测速，完成后自动切换至该子集内最优节点。F4 已与 F1/F2 一并收归后端统一选点命令（前端只发信号、不再自行测速或选点）。具体要求：
- **范围限制（选点限定子集，测速覆盖全量）**：后端对 mihomo 内核全量测量组 `PROXY__METRICS`（url-test 类型、与 PROXY 同成员，覆盖全量节点）统一测速，再在**传入的可见子集内**挑最快节点切换——「所见即所选」成立，「所见即所测」因内核组固定成员覆盖全量不字面成立（已与 v2.6.6 测量层根因修复对齐）。选点严格限定在子集内，绝不跨到子集外比较；F1（不传子集=全量）从全量挑最快，F4 从可见子集挑最快，明确区分。前端仅在触发瞬间把可见节点标「测速中」驱动动画，真实测速与选点在后端完成。
- **并发测速与反馈**：点击后委托后端对该子集并发测速，经全局互斥避免与 monitor/其它触发叠加过量并发；可见待测节点立即进入流光动画，后端结果回传后停止并更新延迟；结果逐节点写回渲染无需等整组结束；测速进行中忽略重复点击；全局并发互斥保证任何并发触发只有一个在执行。
- **最快健康节点判定与自动切换**：子集内取延迟有效（≥30ms 且 <2000ms）者按延迟升序取最快（不受前端排序影响）；切换至该最快健康节点并持久化用户选择；无健康节点则保持当前。整个过程静默，不弹完成通知。

### 5.6 后台测速前端回传
前后端所有群发测速结果（monitor 常驻或前端委托）都必须回传前端使延迟实时更新。后端在统一选点入口内部通过统一事件回传，触发场景包括：启动首次选点（窗口不可见时）/ 进入轻量模式 / 故障自愈 / 前端委托（测速所有按钮、Profile 切换、窗口唤醒静默填充）。前端不再自行发起对 PROXY 组的批量测速，回传成为 UI 延迟数据唯一来源。
- **事件通道**：后端测速完成后统一以 Tauri 事件推送到前端，所有调用路径一致；事件负载包含全部被测节点延迟（含死节点/超时/错误），UI 既能显示健康也能显示失效。
- **前端注入**：暴露批量结果写入方法，逐条写缓存并触发逐节点刷新 + 分组级刷新。
- **监听器随布局组件生命周期注册/注销**，与现有前端测速通路解耦。
- **互不阻塞**：monitor 与前端命令在后端共用同一入口（全局互斥），前端共用同一缓存（JS 单线程安全），动画帧批量合并确保不产生刷新风暴；前端不再直接调用测速 PROXY 组。

### 5.7 导航栏节点轮换
解决点击节点名轮换时切换范围未受当前搜索/过滤/排序子集限制的故障：
- **实时提取过滤与排序状态**：轮换点击事件处理中严禁使用任何缓存的静态节点列表，必须在触发第一时间直接从本地存储解析出当前搜索关键字、隐藏标志、排序规则等最新状态。
- **实时重组候选子集**：用与节点列表页相同的过滤排序逻辑，以最新状态为条件对全量节点实时过滤排序，动态计算当前候选子集。
- **状态兜底轮换**：按「非超时节点优先轮换，全部超时时在候选子集循环」逻辑轮换，轮换后写入配置生效。

### 5.8 重启后选点防丢失
重启程序或导入/切换订阅后，内核重载配置会将活跃节点重置为订阅列表默认首个节点（常是广告假节点）导致断网。必须在配置重载完成后自动触发一次全节点测速并选最快可用真实节点纠正；该动作必须在重载后可靠执行，即使存在并发自动选点也必须确保前端最终覆盖到正确新配置节点执行一次选点。

---

## 🛡️ 六、安全与健壮性

### 6.1 权限最小化
实施最小权限原则：移除 Tauri 配置中开放的执行/生成/杀死/标准输入写等 shell 权限（前端不应能执行任意系统命令）；FS 作用域从通配收紧为仅应用数据目录（防前端读任意文件）；HTTP 插件从允许所有域名收紧为仅已知域名白名单；Asset Protocol 为兼容本地静态资源已回滚为允许任意目录（此条为兼容性例外）。

### 6.2 URL 输入验证
对所有用户可控 URL 输入严格验证，防 SSRF/路径遍历/协议滥用：
- 打开网页仅允许 http/https，拒绝 file://、javascript: 等。
- ZIP 解压用安全解压函数替代原生解压，拒绝含 `..` 或绝对路径条目（Zip Slip 防护）。
- 订阅 URL 在清洗后增加 SSRF 检查，禁止回环/私有 IP/未指定地址。
- 图标下载 URL 增加协议验证与 SSRF 防护。
- 启用忽略证书校验时记录 warn 级日志提醒风险。
- JS 脚本传递用结构化序列化替代字符串拼接，防代码注入。

### 6.3 运行时沙箱
增强 JS 引擎运行时安全：执行用户脚本前冻结各原型链防原型污染；保留循环迭代上限、超时、输出大小上限、JSON 大小上限等已有防护。

### 6.4 防 DoS 与编译加固
- YAML 大小限制：订阅解析前检查大小，拒绝超 50MB 超大 YAML（防解压炸弹与内存耗尽）。
- `overflow-checks` 在 release 配置由 false 改 true，防整数溢出导致安全漏洞。

---

## 📊 七、数据流与 API

### 7.1 单一 PROXY 组模型
采用唯一 PROXY 组设计，后端保证配置中始终存在 PROXY 组；前端数据流严禁引入原版多代理组复杂逻辑：
- 数据计算函数返回值精简（不返回 direct/reject/proxies，严禁按原顺序重排 groups 或构建扁平 proxies 列表）；PROXY 组创建仅 1 层。
- 渲染列表节点仅从 PROXY 组读取，严禁多层 fallback。
- 数据轮询统一由数据层配置驱动，严禁组件中重复定义轮询。
- 代理组名固定为 `'PROXY'`，严禁动态查找。
- 节点选择恢复单组化：前端与后端只处理 PROXY 组，严禁遍历多组（`selected` 数组始终只含一项 `{name:'PROXY', now:'...'}`）。
- 内核重启前后 PROXY 组节点状态保持：内核重启会重置 Selector 的 `now` 为列表首节点（常是广告假节点），重启内部必须在停止前快照 `now`、启动后等就绪恢复；热重载不会重置 `now` 无需保存恢复；所有调用重启处自动受益。
- 自动选点成功后必须回写 `profile.selected`：否则唤醒/进入轻量时恢复会用过时 `selected.now` 把节点切回旧节点（可能不在当前子集），导致 PROXY 真实切换到错误节点数秒到十余秒。回写后所有选点路径自动受益，前端无需重复实现。

### 7.2 订阅导入与内核就绪等待
导入新订阅后重启内核需数秒至十余秒初始化并下载 provider 数据。为确保前端节点表可靠显示：
- **重启后轮询等待**：重启后严禁立即单次刷新作最终数据源，须启动轮询（每 1.5 秒、最多 20 秒）检查 PROXY 组是否含真实节点，就绪方退出；轮询期间加速下载与缓存刷新。
- **缓存直写与 UI 同步**：轮询结束须将最新完整节点数据直接注入本地缓存，避免后续刷新因缓存节流被跳过。
- **前端兜底轮询增强**：空节点轮询恢复在刷新代理数据时必须同步刷新 providers（数据计算依赖 provider 映射）。

### 7.3 订阅与配置文件管理
为解决订阅卡片无法右键操作/无编辑对话框，并保持 6 皮肤视觉融合：
- **右键菜单拦截与触发**：订阅卡片容器绑定自定义右键菜单，屏蔽默认菜单并捕获对应配置项与坐标，在光标处精确弹菜单。
- **菜单功能**：编辑（打开对话框）/ 编辑文件（系统默认编辑器打开本地 YAML）/ 复制链接（本地文件禁用）/ 更新（本地文件禁用）/ 删除。
- **编辑对话框**：MUI Dialog；字段含配置名（全类型可编）、订阅链接（仅远程可编）、更新周期小时（仅远程可编）；保存时写本地配置并触发重建使变更即时应用、关对话框、弹成功通知。
- **菜单与对话框 6 皮肤适配**：Paper 与 Dialog 须归属主题面板类名，背景/模糊/投影/3D 边框/霓虹与当前皮肤及滑块完全联动；菜单项 Hover 高亮应用皮肤样式；输入框/按钮应用 3D 拟物样式并确保换肤刷新。
- **Allow LAN 等开关回弹修复**：根因为内核 API 返回的某参数运行时以小写表现，而原后端枚举缺乏小写转换器注解导致拉取基础配置时反序列化全面报错、前端状态被重置为 undefined。须对该枚举加小写序列化标记（Rust 与 TS 双侧对齐），避免类型不匹配或反序列化失败导致开关集体回弹；允许升级本地插件版本但仍须强制保留小写反序列化 Patch。
- **输入框右键菜单（生产环境粘贴失效修复）**：订阅链接输入框须拦截鼠标右键阻止默认行为并记录坐标，显示自定义右键菜单（剪切/复制/粘贴/全选/清空）；粘贴依次尝试剪贴板接口与 Web 标准接口，精确获取选区起止点插入文本并更新光标；复制/剪切从选区读写剪贴板。

### 7.4 布局组件状态同步
为解决主布局单体过大、渲染开销高、难维护，实施模块化拆分：
- **单一职责与大组件拆分**：拆分为职责单一子组件（节点状态卡、流量面板、订阅导入卡、系统代理接管卡、路由偏好卡、基础设置卡、皮肤卡、连接管理面板、帮助菜单与弹窗等），统一收拢于布局子组件目录。
- **样式与公共辅助抽离**：零散定义的 3D 控件样式/延迟等级映射/版本比对等公共逻辑提取至独立样式辅助模块，通过模块化语法引入。
- **父子状态传递与类型对齐**：主布局统一维护全局共享状态作属性传入子组件；子组件可能为空的状体变量须定义为可选类型严格对齐父组件；回调接口须兼容底座全局组件，严禁缩减签名。
- **本地缓存纯净读取防污染**：严禁在视图组件渲染路径上同步调用本地持久化读取（违背纯函数式设计，致状态回退/性能退化）；任何挂载时获取的本地状态必须包裹在状态初始化钩子的惰性初始化函数中，确保本地 I/O 仅首次挂载执行一次。

### 7.5 策略分流倾向文案
所有「规则可调」选项的 Tooltip 中文须统一为「在预设规则的基础上任意调整路径控制」，并在代码中设为 Tooltip 默认备用文案；须在全部 13 种语言翻译配置中同步更新对应翻译。

### 7.6 编码约束
- `debounce` 工具回调参数须约束为无返回值的固定签名，禁泛型暗示返回值。
- proxy 映射禁止双断言类型转换。

---

## 🔧 八、工程规范

### 8.1 发布流程与版本管理
为符合真实用户测试确认时序、防未验证代码抢跑发布：
1. **发版前（开发自测）**：涉及 Bug 在 bug 列表中只登记于顶部「待验证」区，状态标 `代码已修正，待用户确认`，严禁提前录入底部历史归档。
2. **发版中（编译构建）**：编译打包并发布远程 Tag 时 Bug 保持在顶部「待验证」；发布预检脚本对此状态放行，允许打 Tag 发包供用户下载。
3. **发版后（人工确认归档）**：用户测试确认修复无误后，AI 助手方可归档——删顶部待验证卡，移入底部「已解决历史索引」，状态 `代码已修正，已确认`。

### 8.2 后台资源清理
- **WebSocket 日志协程生命周期熔断**：后台常驻日志协程须感知数据分发通道关闭状态，通道关闭时主动退出，随 WS 关闭自动销毁协程。
- **本地连接池空闲管道激进清空**：清理任务中除断开所有 WS 连接，还须彻底清空全局连接池中保留的空闲管道连接，消除轻量模式下最后几处系统活跃句柄占用。

### 8.3 窗口事件处理
为解决 Windows 下自动轻量模式开启时关闭窗口导致 Panic、托盘消失、内核残留：
- **框架窗口事件接口级别拦截**：禁止在应用运行事件循环中直接捕获关闭请求并调用阻止关闭 API；所有窗口生命周期拦截（关闭请求、聚焦等）必须通过框架提供的窗口事件注册接口统一注册。
- **获取窗口句柄后隐藏**：在窗口事件回调中须安全获取窗口句柄后调用阻止关闭并对该句柄调用隐藏，再执行异步轻量转换，确保窗口真正进入销毁前已完成阻止与隐藏，规避底层「状态已销毁」Panic。

### 8.4 Config Draft 时序
配置 patch 中「应用」须在「增强」之前调用，使读取能拿到最新值；若增强失败通过回滚恢复旧配置。因调用路径为同步命令不存在并发读者，风险可控。

---

## 附录 A: Bug ID 映射表

| Bug ID | 对应章节 | 简述 |
|--------|---------|------|
| BUG-057 | 7.2 | 导入订阅后内核重启就绪等待 |
| BUG-065 | 5.7 | 导航栏置顶出口节点实时子集轮换 |
| BUG-070 | 1.4 | 管理员模式下双内核进程泄漏 |
| BUG-071 | 3.3 | 客户端与内核自动更新 |
| BUG-072 | 7.3 | 订阅/配置文件右键菜单与编辑 |
| BUG-074 | 3.3 | 内核更新多重降级代理 |
| BUG-075 | 5.4 | 系统资源与 I/O 占用优化 |
| BUG-078 | 3.3 | 内核更新下载超时 Fallback |
| BUG-079 | 7.5 | 策略分流倾向气泡文案统一 |
| BUG-080 | 3.3 | 相同版本提示规范 |
| BUG-082 | 8.2 | WebView2 后台内存回收 |
| BUG-083 | 5.1 | 后端常驻活跃节点监控 |
| BUG-089 | 已废弃 | 后台节点延迟监测（被 5.1 替代） |
| BUG-090 | 2.5 | Trump-3D 深色模式对比度 |
| BUG-091 | 7.3 | 订阅链接输入框剪贴板交互 |
| BUG-092 | 5.5 | 闪电按钮批量测速 |
| BUG-093 | 5.1 | 后端自动选点与端口安全 |
| BUG-102 | 7.4 | 布局组件单体化与状态同步 |
| BUG-138 | 5.5 | 批量测速自动优选 |
| BUG-174 | 2.5 | Retro-3D 卡片文字颜色 |
| BUG-175 | 4.1 | 窗口关闭即入轻量模式 |
| BUG-176 | 5.6 | 后台测速结果前端回传 |
| BUG-217-238 | 8.2 / 2.5 | 全面代码审计修复 |
| BUG-240 | 2.6 | 设置面板瞬时挂载与联动关闭 |
| BUG-256 | 8.3 | 窗口生命周期事件拦截 |
| BUG-257 | 4.3 | 后台监测周期恒定（轻量模式不再放慢，设计确认） |
| BUG-258 | 4.2 | 轻量模式进入时连接清理 |
| BUG-259 | 4.2 | 轻量模式进入时数据订阅熔断 |
| BUG-262 | 4.4 | 轻量模式唤醒后节点延迟刷新 |
| BUG-269 | 8.2 | WebView2 销毁与 WebSocket 泄漏 |
| BUG-272 | 5.2 | 10 分钟节点切换冷却 |
