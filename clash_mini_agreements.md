# Clash Mini 权威开发协议 (Authorized Development Agreements)

本协议是 Clash Mini 项目开发与优化的唯一权威指南。所有功能开发、界面微调和行为逻辑必须严格遵守本协议。本协议将随代码一同进行 Git 版本管理。

### 👑 【最高设计基准 (Highest Design Principle)】
**除本协议特别指定的「两张专属表格」以及「宏观界面布局尺寸」外，所有 UI 元素（包括但不限于按钮的外观/大小/圆角、输入框的高度/填充、选择/切换控件、全局字体与字号、深浅色主题配色体系等）一律沿用原程序 Clash Verge 的默认设计风格与表现，禁止任何别出心裁的自定义设计或硬编码色彩（如硬编码 `#1976d2` 蓝色等样式必须全部移除，统一交由原程序 MUI 主题机制管理）。**

### 🚨 【修改顺序铁律与避坑把关（开发前必读）】
**任何针对本项目的功能开发、Bug 修复、UI 细节微调或系统配置变动，必须遵循「协议与避坑查阅优先」的硬性流程：**
1. **查阅红线**：每次开发前，必须先查阅项目避坑红线文件 [clash_mini_pitfalls.md](file:///c:/Users/sun_y/Documents/AntiGravity_Projects/ClashVerge/clash_mini_pitfalls.md)，确保绝对不触犯静默运行命令等底线错误。
2. **先改协议**：在触碰任何代码（TS/React、Rust、JSON 等）前，必须首先在《唯一协议》（本文件）的对应章节中，将相关修改的设计规范、端口分配或行为变更写入协议。
3. **后改代码**：保存协议更新后，方可编写代码。代码的实际实现必须与更新后的协议条款保持 100% 吻合。
4. **流程审计**：任何未在协议中备案的代码修改，或未经用户在对话中明确允许而执行的代码操作，均视为严重违规。

---

## ⚙️ 一、 项目定位与系统隔离规范

为了保证 Clash Mini 与原版 Clash Verge 在同一台 Windows 机器上**完美并存、互不干扰、流畅运行**，必须在前端和打包配置中实施彻底的命名和配置隔离，严禁触碰 Rust 后端核心。

1. **开发边界**：
   * 主要针对**前端（TypeScript/React）**和打包配置进行修改。当前后项需求产生冲突时，遵循**后项规定优先（后方优先）**方针，允许对 Rust 后端代码（如 `mod.rs`、`menu_def.rs` 等）进行局部调整与重构，但仍需确保不修改后端核心业务架构与内部稳定性。
2. **显示名字与内部代号**：
   * 前端所有面向用户的展示界面，软件名称必须精确显示为：**`Clash Mini`**（注意大小写和空格）。
   * 程序打包（如 `tauri.conf.json` 中的 product name）也必须使用精确的 `"Clash Mini"`。
3. **数据与配置文件隔离**：
   * 必须保证 Clash Mini 与原版的配置相互独立。配置文件（如 `clash-verge.yaml`）、数据库以及订阅列表（`profiles.yaml`）在用户本地 of `AppData\Roaming\clash-verge` 目录中独立加载（沿用原版后端逻辑，以防破坏后端稳定性）。
4. **网络端口避让**：
   * 默认 Mixed 混合代理端口设为 `10801`，Controller API 端口设为 `9098`，彻底避让原版默认端口，允许两个客户端同时在线。
5. **系统服务共享机制**：
   * 保持系统服务注册名 `clash_verge_service` 不变。新用户启动 TUN 模式时自动触发 UAC 安装该服务；若原版已安装，将通过 SCM 共享管道接管服务运行独立的 Clash 核心配置，实现无缝切换。
6. **界面语言单一化**：
   * 软件界面一律仅保留简体中文，彻底废除多语言切换选择框与非简体中文语言资源。
7. **禁用自动更新**：
   * 彻底禁用检测及提示官方 Clash Verge 新版本的 updater 自动更新机制与接口，防止用户因升级覆盖而损坏项目独立性。
8. **单实例检测端口隔离**：
   * 为了彻底实现多客户端完美并存运行，Clash Mini 的单实例检测端口在 Release 模式下设为 `33335`，在 Dev 模式下设为 `33336`。绝对禁止使用与原版相同的端口（`33332`/`11234`），从而彻底杜绝因单实例检测机制互锁而产生的冲突和闪退。

---

## 🎨 二、 界面布局与视觉规范

主界面窗口必须采用紧凑扁平的精致质感，拒绝粗大臃肿 of 现代 UI。

1. **双层常驻布局**：
   * 界面不设侧边栏/多页面导航。窗口常驻分为上下两层：
     * **上半层（自适应高度，flex: 1）**：展示“当前活跃节点”以及下方的“节点与延迟测试列表”。
     * **下半层（固定 178px 高度，常驻）**：展示全宽的流量监控折线图与指标数据。该固定高度确保流量折线图能够完全恢复为原版 Clash Verge 的 130px 物理高度，彻底解决高度压缩导致底部时间轴文本与“Smooth”和“Points”等状态文本发生重叠的问题。下方的四个数据指标卡片设计为横向单行排列，且采用微凸起、立体按钮感（高 24px，圆角 6px，带有上边缘微高光与外阴影投影）的精致卡片设计，并拥有与折线图曲线颜色相呼应的背景与边框色（上传指标为淡琥珀金色调，下载指标为淡电光蓝色调）。
2. **滑出设置面板**：
   * 点击右上角齿轮图标，设置抽屉往左下方滑出，**刚好完全覆盖上半层区域**，而**下半层（178px 区域）的流量与图表依然露出来并保持可见 and 高频更新**。
   * 设置抽屉子窗口的边框采用 **`4px double`** 双实线设计，与主界面节点表格的中缝双线相呼应。
   * 设置面板内采用**左右分栏**布局：
      * **左侧分栏（固定为 200px 宽度）**：自上而下整齐摆放「机场订阅区」、「流量接管模式选择」与「极简设置项」，不随窗口宽度变化拉伸。其中，为了解决标题字号反而显得小的问题，抽屉内所有设置项标签（如“开机自动启动”、“启动时最小化”、“通知弹窗显示”、“主题模式”、“流量接管模式”、“分流策略倾向”、“Mixed Port”等）的字号一律统一为 `11px`，与各自模块标题字号（`11px`）保持一致。同时，为了使三个开关按钮（开机自动启动、启动时最小化、通知弹窗显示）与小文字视觉比例协调，Switch 控件采用 `transform: 'scale(0.75)'` 进行整体缩小，并采用 `transformOrigin: 'right center'` 靠右对齐。
      * **右侧分栏（自适应 flex: 1 宽度）**：完整放置「路径控制（原连接审计中心）」，随着窗口宽度自适应拉伸。右侧分栏顶部的标题文本缩减为：**「路径控制（右键点击链接）」**，缩减字符以适应紧凑窗口排版。
3. **原生标题栏与避让**：
   * 窗口必须保留 Windows 系统的原生边框和标题栏 (`decorated: true`)。为了在原生标题栏中展现粗体，强制使用数学无衬线粗体字符 `𝗖𝗹𝗮𝘀𝗵 𝗪𝗶𝗻𝗟𝗶𝘁𝗲` 写入 Tauri 窗口的 title 属性。
   * 窗口启动时的默认尺寸硬性设定为：宽度 640 px，高度 860 px。
   * 设置齿轮按钮以绝对定位放置在客户区最右上角，宽度 and 高度固定为 `38px`，与上方的原生标题栏底部及右侧窗口边缘保持零间距贴合。
   * 置顶的「当前活跃出口节点状态显示条」右侧必须增加间距（`marginRight: "48px"`），防止其与右上角设置按钮发生重叠。
4. **字体与字号**：
   * **清除拟物特效**：彻底禁用和清除所有拟物化视觉特效，包括按钮/卡片的渐变高光层（sheen）、悬停时的对角线扫光动画、文字阴影（text-shadow）、水晶发光阴影及呼吸灯闪烁动画，确保界面完全呈现出平整实体、现代扁平化（Flat Design）的简约风格。
   * **禁用窗口透明与虚化**：在 Rust 窗口配置（`window.rs`）中将 `transparent` 设为 `false`，彻底注释/移除 `apply_mica`、`apply_acrylic` 和 `apply_blur` 等窗口虚化效果的注入，确保原生窗口完全不透明。
   * **清除虚化与半透明**：彻底清除所有 CSS/SCSS 样式文件以及 React 组件属性中的虚化滤镜。废除所有半透明（如 `rgba(...)` 或 `alpha(...)` 比例）悬浮及面板背景设定。将主背景（`--aero-bg`）、设置抽屉与面板（`--aero-panel-bg`）、下拉菜单、弹出对话框、右键上下文菜单及详情面板的背景色全部提升为 **100% 实体纯色不透明**（例如亮色模式使用 `#ffffff` 或 `#f0f5ff`，暗色模式使用 `#1e2438`）。
   * **保留经典分割线结构**：特别保留用于结构分隔的表格 2px 网格线、中缝的 5px double 双实线边框，以及设置抽屉的 4px double 双实线边框，仅剥离其表面的拟物发光和透光特效。
   * **【MUI 语义化与 100% 肃清 Aero 变量】**：在 `_layout.tsx` 中移除所有 `aero-crystal-card`、`aero-crystal-btn` 等 className 引用，三个设置分区卡片背景由 `rgba` 半透明替换为 MUI `divider` 语义边框。彻底从所有 TSX 业务代码（如 `_layout.tsx`、`connection-table.tsx`）中肃清和废除 `var(--aero-border)`、`var(--aero-panel-bg)`、`var(--aero-bg)` 等残余 Aero 变量，统一使用 MUI 标准的 `theme.palette.divider`、`theme.palette.background.paper` 和 `theme.palette.background.default`。在全局 SCSS 样式（`index.scss`, `layout.scss`）中，同样使用系统级标准 CSS 变量（`var(--divider-color)`, `var(--background-color)` 等）进行替换，实现主题控制的 100% 扁平语义化。
6. **流量折线图配色、尺寸同步与防挤压优化**：
   * 流量监控折线图（Canvas 渐变绘制）的上传（琥珀金 `#D4AF37`）与下载（电光蓝 `#0084FF`）必须采用标准十六进制颜色。禁止在动态生成渐变时使用 rgba 字符串做拼接，防止由于 alpha 字段拼接不规范导致 Canvas addColorStop 抛出 SyntaxError 闪退。
   * 简化渲染判定逻辑，仅在 Document 处于 Hidden 状态下休眠。只要窗口可见，即使数据暂时没有更新或窗口未激活，在发生大小变化或重绘调用时也必须保证 Canvas 尺寸与 CSS 属性实时同步。
   * 图表的上下左右内边距：顶部边距设为 `10px`，右侧边距设为 `10px`，底部边距设为 `22px`，左侧边距设为 `30px`；时间轴 Y 轴位置自适应调整为 `height - padding.bottom + 12 = height - 10`。
   * 底部流量数据卡片（Metrics Row）：4 个小卡片按照“上传组（速度、总量）”、“下载组（速度、总量）”左右进行排布，共享组内边框与底色配色（上传琥珀色，下载蓝色）但物理上仍为独立卡片（有 gap 间距和独立圆角边框）。首个卡片写明上传/下载，后续卡片为总量（如上传:、总量:、下载:、总量:），图标（`↑` / `↓`）必须严格放在文字前面。所有卡片容器及文本组件全部设置 `whiteSpace: 'nowrap'` 强制防折行。
7. **输入框与按钮布局细节**：
   * Mixed Port 端口输入框：宽度固定为 `60px` 以隐藏浏览器原生数值增减小箭头，输入及描述布局保持原规格。
   * 订阅链接输入框与导入按钮：采用**两行布局**，第一行为全宽订阅输入框（Placeholder 为 `"填入订阅链接 (YAML)"`），第二行为自适应宽度的导入按钮（按钮文字为 `"导入订阅链接（YAML）"`），且在父级容器中**水平居中对齐**。
   * 所有的按钮、输入框、选择控件的外观颜色、高度、边框和状态表现，**一律沿用原程序 Clash Verge 的默认设计风格**，禁止任何硬编码的自定义配色（如之前硬编码的 `#1976d2` 等需全部清理，统一跟随 MUI 默认主题系统）。“导入订阅”与“系统调试运行日志”按钮均使用主按钮风格（`contained`），且宽度均为自适应并水平居中。
8. **基础设置模块排版与外观主题**：
   * “基础与自适应设置”卡片重命名为“基础设置”，内部子项和间距微调保持紧凑。
   * 系统日志按钮：将系统运行日志查看整合为居中的“系统调试运行日志”主按钮，其外观表现完全使用原程序默认主题样式。
   * 主题模式切换：在基础设置列表中，在一行内展示“主题模式”标签与并排三选一按钮组（系统/浅色/深色），按钮组进行极窄化适配以避免折行，按钮高度为 22px，字号统一为 10px。
   * **界面华丽度双控制系统（双滑动条）**：在“主题模式”下方增加两个滑动条组件（Mui Slider），标签分别为“立体磨砂 (Depth)”与“色彩霓虹 (Vibrancy)”。数值范围为 `0.0` 至 `2.0`（步长为 `0.1`，默认值 `1.0`），数据在 LocalStorage 中持久化保存，并在 React 中监听变化，通过 `document.documentElement.style.setProperty` 将变量 `--depth-factor` 和 `--vibrancy-factor` 动态注入至最顶层 `:root`，用以全局联动控制光影与发光效果。
   * 订阅列表卡片造型：采用原版精细卡片设计，背景色亮色固定为 `#ffffff`，暗色固定为 `#282A36`。激活项左侧包含 3px 宽的 `primary.main` 左侧主色边条，文字高亮为 `primary.main`，未激活项左侧透明。保留已用/总流量数据、过期时间以及底部高 2px 的主色进度条（若 `total > 0`），字号微调为 11px（名称）和 9px（数据）以防折行。
   * 设置与关闭按钮冲突解决：删除抽屉内部自带的独立 Close 关闭按钮，改在右上角设置齿轮按钮通过 `drawerOpen` 状态二合一动态切换图标（打开时显示 CloseRounded 叉号，关闭时显示 SettingsRoundedIcon 齿轮），激活时背景色与边框均改用 `theme.palette.primary.main` 配色系统。
9. **主页专属表格：主界面节点列表（三列表格）网格设计**：
    * 表格高度与布局：由原本的每行 2 个节点升级为每行 3 个节点的紧凑布局（即三列排版），单个节点高度固定为 `20px`。
    * 节点信息精简与列剪裁：每个节点内部彻底裁剪并移除原有的“协议/类型”列，只保留“节点名称” and “延迟数值”两列，大幅清空非必要的杂乱文字。
    * 属性字体调整：节点列表中的节点名称字体大小设为 `12px`，延迟数值的字体大小设为 `11px`，选中时名称字体加粗。
    * 网格线加粗：表格内部的所有水平分隔线、垂直列分隔线宽度设为 `2px solid`，且完全使用 MUI `theme.palette.divider`。
    * 列间纵向双线划分：为突出三列排版的对称美感，在第一列右侧、第二列右侧的中缝，使用宽度为 `5px double` 的 vertical 双实线边框（`5px double var(--theme-border)`，确保在所有主题色下清晰可见），实现明显的隔断感。
    * 延迟列居中对齐与宽度：延迟数值那一列（每个节点的最右侧）的宽度由原本的 `50px` 调整为 `65px`，以完全容纳 `"Timeout"` 和 `"testing"` 等长文本，对齐方式改为居中对齐（`justifyContent: "center"`）。
    * 表格外周竖边框：表格最外侧的左、右两条竖边框线由原本的单实线变更为 5px double 双实线，与表格内部的纵向双实线分割边框保持一致。
    * 表头固定行显示：顶部的固定操作行（即 `ProxyHead`，包含定位、延迟测试、排序、搜索过滤等图标按钮）在「规则模式」、「全局代理」和「全局直连」模式下均必须保持显示，绝对禁止在非规则模式下隐藏。
10. **全局滚动条隐藏与无痕滚动**：
    * 为了最大程度保留界面显示空间、契合 Mini 极简扁平定位，全局彻底隐藏所有页面的滚动条（包括主页面、设置面板、订阅列表、连接列表等）。
    * 通过 CSS 的 `::-webkit-scrollbar { display: none !important; }` 和 `scrollbar-width: none !important;` 规则实现无痕隐藏，但必须确保内容区域仍可通过鼠标滚轮、触控板手势、键盘方向键正常滑动。
    * 同时，移除 `.base-page .base-container > section` 的 `scrollbar-gutter: stable;`，使内容排版充分利用窗口的全部水平宽度，防止右侧留下空白轨道占位。
11. **极光背景与 3D Bevel 视觉层（阶段三）**：
    * **极光动态背景**：在 `.layout` 容器底层通过 `::before` 伪元素实现一个 Slow-Motion 慢动作极光背景。极光使用三个径向渐变中心（包含 `--primary-color-rgb` 颜色、电光蓝 `#0084FF` 和琥珀金 `#D4AF37`），通过 `@keyframes` 旋转、缩放与位移动画实现飘逸流转。极光整体的不透明度与发光强度通过 `var(--vibrancy-factor)` 线性缩放（当值为 0.0 时完全不显示，为 2.0 时达到最强视觉发光）。
    * **毛玻璃材质与 3D 浮雕（Bevel）光影**：
      * 左侧分栏 `.layout-content__left` 和右侧分栏 `.layout-content__right`，以及设置抽屉 `.theme-panel` 和当前节点卡片 `.theme-crystal-card` 的背景，将分别与 `--depth-factor` 联动变为半透明（使用 `rgba` 与 `--theme-bg-base-rgb` 和 `--theme-panel-base-rgb` 进行计算，当 depth 为 0.0 时 100% 实体纯色，当增加时透明度渐进提高）。
      * 同时通过 `backdrop-filter: blur(calc(8px * var(--depth-factor)))` 提供磨砂毛玻璃透光材质感。
      * 所有的卡片、分栏和面板内侧引入由 `--depth-factor` 控制的 `box-shadow` 内发光与内阴影：左上角斜向拉入微弱白高光（`inset calc(1px * var(--depth-factor)) calc(1px * var(--depth-factor)) calc(2px * var(--depth-factor)) rgba(255, 255, 255, calc(0.12 * var(--depth-factor)))`），右下角斜向拉入微弱深阴影（`inset calc(-1px * var(--depth-factor)) calc(-1px * var(--depth-factor)) calc(2px * var(--depth-factor)) rgba(0, 0, 0, calc(0.3 * var(--depth-factor)))`），整体底部渲染跟随 depth 深度比例放大的毛玻璃投射黑影，并由 `var(--vibrancy-factor)` 控制外围霓虹背光（neon glow）效果。
12. **代理节点 Hover 动效与流量卡片 3D 光影（阶段四）**：
    * **代理节点悬浮动效**：每个代理节点单元格卡片（`ProxyItem`）在鼠标悬停（Hover）时，触发 `transform: translateY(-1.5px)`，并施加由弹性物理曲线（`cubic-bezier(0.175, 0.885, 0.32, 1.275)`）控制的物理回弹与上升手感。同时，触发一次快速扫过的光影扫光（通过 `linear-gradient` 的 background-position 在 0.6s 内完成单次 `hoverSweep` 动画），提升视觉交互深度。
    * **底栏数据卡片 3D 反射**：底部的四个数据卡片（上传速度、上传总量、下载速度、下载总量）的背景不透明度、边框色彩强度和 3D 立体反射投影参数，通过 CSS 变量 `var(--depth-factor)` 和 `var(--vibrancy-factor)` 进行深度关联与动态比例缩放。在 Hover 时，卡片同样产生微幅抬升（`-1px`）并自动增强发光度与背光阴影，实现奢华仪表盘的触觉交互反馈。
13. **VGA 640x860 等比例放大适配与全场景 3D 浮雕控件（v1.0.9 阶段）**：
    * **等比例缩放适配**：为匹配 640px 窗口，将左侧设置抽屉宽度拓宽为 `240px`；设置标题与设置项标签字号统一调整为 `13px`；订阅列表名称字号提升至 `13px`，流量及过期时间字号提升至 `11px`；Switch 开关缩小比例放宽为 `scale(0.9)`；主要控制按钮高度升级为 `34px`，文字字号提升至 `13px`；Mixed Port 输入框宽度调整为 `80px`，字号调整为 `13px`，整体组件视觉比例保持和谐协调。
    * **3D 按钮触觉动效**：大大小小的按钮（导入、日志等）常态下具有 3D 浮雕投影与 Bevel 立体发光边框，Hover 时整体抬升 `1.5px` 并增大投影范围以展现磁吸浮空感，点击（Active）时下沉 `1px`，收缩外阴影并反转为下陷内阴影，提供实体按键点按的触觉反馈。
    * **3D 三态滑动器**：底槽渲染为向内凹陷的 3D 滑槽（Groove），而滑动选项块渲染为向外凸起的立体滑动滑块（Block），配合平滑的移动滑块微动画。
    * **3D 拨动开关（Switch）**：定制重构 base-switch，使其滑轨（Track）表现为内凹 3D 暗槽，滑块（Thumb）表现为凸起的 3D 悬浮球，支持悬停亮度变化，彻底淘汰扁平控件。
    * **3D 指标小卡片强化**：增加左上白高光、右下深阴影的双向立体 Bevel 物理反射，外阴影模糊距离扩展为 `6px`，使其立体感真实可信。


---

## ⚙️ 三、 路由逻辑与「手动路径控制」

1. **流量接管模式三态互斥单选**：
   * **术语规范**：废除“TUN 虚拟网卡”词汇，统一规范使用**「TUN 模式」**。
   * **互斥逻辑**：彻底取消原程序中“系统代理”与“TUN 模式”可同时开启的混乱设计，重构为**三态互斥单选**，系统仅存在以下三种互斥状态之一：
     1. **手动模式 (Manual)**：系统代理 = 关，TUN 模式 = 关。此时 Clash 核心在后台运行并监听本地端口（如 10801），但不修改系统网络设置，需用户手动配置特定应用的代理。
     2. **系统代理 (System Proxy)**：系统代理 = 开，TUN 模式 = 关。
     3. **TUN 模式 (TUN Mode)**：系统代理 = 关，TUN 模式 = 开。
   * **出厂默认值**：无配置文件或首次启动时，根据极简、开箱即用的设计方针，默认处于**「系统代理」**，确保免 UAC 且零配置即用。
   * **服务与提权失败回弹**：当用户切换到「TUN 模式」且后台检测到需要安装系统服务时，如果由于用户取消 UAC 授权或提权失败导致服务未成功运行，程序在前台弹出错误提示的同时，必须将 UI 交互选择器自动回弹退回到切换前的状态（「手动模式」或「系统代理」）。
   * **UI 交互表现**：将原有的二选一 ButtonGroup 升级重构为精致的**三段式滑动分段选择器 (Segmented Control)**，包含「手动模式」、「系统代理」、「TUN 模式」三个横向排列的标签，带有平滑的微动画背景滑块指示器。
2. **分流策略倾向 (Routing Policy Inclination)**：
   * **设计定位**：为了让网络流量的分流兼具智能与确定性，在设置抽屉中提供“分流策略倾向”选择模块，采用三段式滑动选择器 (Segmented Control)。它取消原程序的“全局直连”和“全局代理”原生内核模式，在底层强制 Clash 核心永远运行在 **`rule` (规则模式)** 下。
   * **三档功能与映射逻辑**：
     1. **直连兜底 (Direct Fallback)**：底层处于 `rule` 模式，匹配置顶的 google/github/telegram 以及 GFWList 等代理规则。未匹配到规则的未知漏网之鱼，在最末尾一律直连兜底（注入 `MATCH,DIRECT`）。
     2. **规则可调 (Rule Adjustable)**：标准的规则分流模式。底层的兜底策略完全尊重用户在订阅配置/路径控制中对各种规则的自定义调节。
     3. **代理兜底 (Proxy Fallback)**：底层处于 `rule` 模式，已知的国内流量和局域网 IP 依然匹配规则直连。未匹配到规则的未知漏网之鱼，在最末尾一律走代理兜底（注入 `MATCH,PROXY`）。
   * **出厂默认值**：为了保证未知与新增外国网站不发生连接超时故障，默认处于**「代理兜底」**。
   * **落地方针**：无论用户选择哪一档，底层的自动规则引擎与用户在连接历史中的右键“手动抓捕 (快速分流)”规则均 100% 生效，防止分流引擎失效。
3. **GFWList 规则源与强规则置顶 (GEOSITE Rules & GFWList Fallback)**：
   * **强规则置顶**：为了 100% 避免国内 DNS 污染并保证核心服务绝对可用，在规则最顶部强制注入 Clash 内核自带 of `GEOSITE` 强域名解析规则，包含 `GEOSITE,google,PROXY`、`GEOSITE,github,PROXY` 和 `GEOSITE,telegram,PROXY`。
   * **GFWList 兜底**：使用自动更新 of GFWList 规则集作为其他被墙网站的拦截与路由兜底，命中后走 `PROXY` 组。为了避免在首次启动时因直连被墙而导致 `gfwlist` 无法下载，硬性在 `gfwlist` rule-provider 中配置 `proxy: PROXY`，强制通过选中的代理节点进行下载。
   * **国内流量直连**：规则最末尾根据「分流策略倾向」决定使用 `MATCH,DIRECT` 还是 `MATCH,PROXY` 确保兜底分流正确。
4. **设置页专属表格：手动路径控制（连接管理）双列设计与滚动条高度约束**：
    * **表格双列裁剪与比例伸缩**：彻底裁剪该表格至仅保留「链接目标 (Host)」与「路由 (Chains)」两列。在「路由 (Chains)」列中仅过滤提取并展示该连接对应为「直连」、「代理」或「封锁」三者之一的直接模式。「路由」列采用固定宽度布局（固定为 50px，始终贴 in 最右侧，居中对齐），左侧的「链接目标」列则采用 `flex: 1` 自适应拉伸。
    * **表格风格与双框对称线**：超紧凑 Excel 风格，行高固定为 `20`，单元格 py 内边距收窄为 `0.5`，文字字号 `12px`，单元格左右边距 `1.5`。外周包裹边框线由 solid 升级为四周统一的 **`5px double`** 双实线（使用 `'var(--theme-border)'` 确保在各主题下清晰可见），标题行与数据行之间的分割线也同步重构为 **`5px double`** 双实线。单元格内部行/列网格分割线为 `2px solid` 实体分割格线。奇偶行使用淡色调的斑马纹。
    * **滚动与高度约束**：手动路径控制的表格外部容器以及其父容器必须设置 `minHeight: 0` 和 `overflow: "hidden"` 属性，防止表格伸长溢出而导致滚动失效。同时将表格的滚动条完全隐藏（使用 `scrollbarWidth: 'none'` 及 `&::-webkit-scrollbar { display: 'none' }`），保持鼠标滚轮滚动正常。
    * **快捷控制**：右键点击任意连接，弹出快捷分流上下文菜单，选项为：**走直连 (DIRECT)**、**走代理 (PROXY)**、**封锁它 (REJECT)**。
5. **唯一代理组与去机场规则化 (Single Proxy Group & Anti-Airport Rules)**：
   * **去机场分组与规则**：Clash Mini 彻底摒弃并过滤机场订阅配置文件中自带的、复杂且不透明的多级代理分组和非公开分流规则。
   * **唯一 PROXY 组**：底层的 Clash 配置只保留一个唯一的代理组（命名为 `PROXY`），该组包含且只包含解析出来的原始服务器节点。
     * **代码级拼写一致性**：所有涉及快捷分流、自动规则写入或备选查询的逻辑中，默认兜底或目标代理组的名称在代码层面必须统一拼写为大写的 `'PROXY'`。禁止在代码中硬编码或返回任何其他拼写方式（如 `'Proxy'`），确保在网络或核心未就绪触发异常兜底时，生成的分流规则（如 `DOMAIN-SUFFIX` 等）的目标组始终能与唯一组 `'PROXY'` 对齐，避免发生内核配置解析错误导致崩溃。
   * **主界面节点表格**：主窗口上半层直接展示这一个唯一的 `PROXY` 组下的所有原始服务器节点。用户在表格中选中某一个节点，即代表当前代理出口切换为该节点，所有走代理的流量均以此节点为唯一出口。
   * **GLOBAL 组的同步绑定与联动保障**：不管处于何种流量接管模式下，系统在界面 and 逻辑上表现为仅有唯一的节点列表（即 `PROXY` 组下平铺的所有物理节点）。为确保在「全局代理」模式下，流量出口依然 100% 同步跟随用户在主列表里选中的节点，后台及前端逻辑必须强制将 `GLOBAL` 组绑定指向唯一代理组 `PROXY`（通过 `selectNodeForGroup('GLOBAL', 'PROXY')` 保证在启动、配置文件更新重载、以及流量接管模式切换时强行生效）。用户在界面上只需切换 `PROXY` 组的节点，流量出口在任何模式下均自动跟随改变，绝不允许因 `cache.db` 的历史缓存或其他操作导致 `GLOBAL` 组与 `PROXY` 组发生脱节。
6. **开机自动启动与 UAC 提权机制**：
   * **提权提示**：如果系统内存在管理员创建的自启动计划任务（即 `Clash Mini (Admin)`），且当前程序并非以管理员身份运行：
     * 用户开启/关闭“开机自动启动”开关时，程序在尝试删除或修改该管理员任务时，必须通过 `runas` 弹出 Windows 官方的 UAC 权限确认框；
     * 用户在提权框中确认后，程序将成功删除旧的管理员任务并建立当前用户的任务，避免权限不足静默失败。
   * **前端 Switch 状态回滚机制**：
     * 前端通过 `patchVerge` 异步接口修改配置（如“开机自动启动”、“启动时最小化”）时，如果后端返回权限拒绝或由于用户取消 UAC 导致接口报错，前端必须捕获异常并调用 `refetch()` 或强行将 Switch 的 checked 状态重置回后端真实的配置，以防止 UI 开关状态与实际配置发生脱节和联动错乱。

---

## 🐱 四、 系统托盘图标规范

系统托盘图标使用指定的布偶猫咪头像图片，并直接从后端打包嵌入 PNG 字节流，以确保和原版彻底隔离：

1. **手动模式 (Manual)**：
   * 采用金属质感灰色猫咪头像：`clash_mini_ragdoll_metal`（`tray-icon.png`）。
2. **TUN 模式 (TUN Mode)**：
   * 采用带星星发光质感的蓝色猫咪头像（`tray-icon-tun.png`）。
3. **系统代理模式 (System Proxy)**：
   * 采用带金色相框的猫咪头像（`tray-icon-sys.png`）。

---

## 📋 五、 18 项用户反馈问题状态跟踪清单

| 编号 | 反馈问题描述 | 状态 | 最新处理与对齐方案说明 |
| :--- | :--- | :---: | :--- |
| **1** | 项目是否使用了 Git 进行版本管理？ | **已解决** | 本地已完全使用 Git 托管，当前在 `dev` 分支。 |
| **2** | 应用版本号问题：当前版本为 2.5.2，建议从 0.0.1 开始。 | **已解决** | 前端发布包版本号已重置为 `0.0.1`，下一个修改发布的版本为 `0.0.2`。 |
| **3** | 流量折线图图表一片空白，下方四个指标颜色杂乱。 | **已解决** | 移除了 Web Worker，改为 React 状态直接 Canvas 渲染。上传指标统一为淡金色/琥珀色，下载指标统一为标准电光蓝/青色，移除拟物光感。 |
| **4** | 「基础与自适应设置」滑动开关选项图标过大。 | **已解决** | 全局 Switch 物理尺寸固定缩减为小巧精致的规格（宽 `32px`，高 `18px`，滑块 `14px`）。 |
| **5** | 下拉菜单与抽屉面板背景透明度过高，导致文字重叠混杂。 | **已解决** | 全局下拉菜单、抽屉、主背景以及全部拟物效果彻底清除，不透明度提升至 100% 纯色。 |
| **6** | 界面词汇翻译生硬，例如“混合监听端口”。 | **已解决** | 统一替换为更加专业且通用的英文标准术语：**`Mixed Port`**。 |
| **7** | 主窗口上半层节点列表展示方式应改成 Excel 紧凑双列表格（选中半行）。 | **已解决** | 彻底废除单列和状态对勾列，重构为每行 2 列，每列展示“名称、协议、延迟”三项要素，高度为 `20px`。每个节点直接以 `ListItemButton` 渲染。选中节点时仅高亮对应的半行单元格。加入微米淡蓝色调的行级斑马纹。表格外周框线加粗至 4px，内部网格线加粗至 2px，第一列右侧中缝分隔线采用 5px double，延迟列宽度调至 65px 并居中对齐，字体大一号。 |
| **8** | 设置面板右上角组件重叠；连接的“活跃/历史”按钮位置不合理。 | **已解决** | 1. 设置抽屉打开时自动隐藏主窗口右上角齿轮。 2. “手动控制路径”的“活跃/历史”按钮与标题同行并排靠左对齐。 |
| **9** | 「查看系统运行日志」按钮排版与周边格格不入。 | **已解决** | 取消右下角/底部突兀的按钮与冗余行，在「基础设置」卡片最下方直接排版为居中的“系统调试运行日志”主按钮（按钮风格完全采用原程序默认）。 |
| **10** | 规则模式下无法代理 Google 等常用站点。 | **已解决** | 在 GFWList 模式最顶部强制注入 GEOSITE 强规则，绕过 DNS 污染，并在背景自动更新。 |
| **11** | 界面术语优化与对称概念命名。 | **已解决** | 1. 将“分流策略”文案改写为“代理策略”；2. 将“直连”模式在代理模式对比命名中改写为“全局直连”，与“全局代理”对称。 |
| **12** | 「手动路径控制」列表排版空旷，需要收紧行距并统一风格。 | **已解决** | 将表格收紧，统一为 Excel 精致表格（12px字体，1.5倍边距，外框线与单元格网格线），奇偶行使用与节点列表一致的淡蓝色斑马纹；右侧滚动条隐藏。 |
| **13** | 连接审计中心行点击详细信息面板定位 Bug（不随行滚动）。 | **已解决** | 点击行时的详细信息抽屉已被修复，能正确定位。 |
| **14** | 连接审计中心功能引导与路由设置说明（右键快捷分流）。 | **已解决** | 重命名界面标题为“手动控制路径（右键点击链接）”，右键菜单中增加走直连、走代理、封锁它的快捷分流指令并注入最高优先级规则。 |
| **15** | 系统托盘右键菜单中保留的「轻量模式」选项点击无响应。 | **已解决** | 从系统托盘右键上下文菜单配置中，彻底移除「轻量模式」这一行选项。 |
| **16** | 主页面置顶的已选中节点信息显示条布局分裂，左右留白过多。 | **已解决** | 取消原左右两侧对齐的设计，将所有文字信息和延迟数据统一在一行内**居中对齐排列**。 |
| **17** | 订阅列表中出现没有名字的空行，删除重启后还会再现。 | **已解决** | 在前端过滤掉没有名字的订阅项。 |
| **18** | 流量接管模式与分流策略倾向的默认出厂位置不合理。 | **已解决** | 默认出厂流量接管模式已变更为“系统代理”，分流策略倾向已变更为“代理兜底”，确保新用户开箱即用且防止连接超时。 |

---

## 📋 六、 版本发布标准作业程序与检查清单 (Release SOP & Checklist)

本章节定义了 Clash Mini 项目发行新版本的标准流程和自动化检查项。在准备发布新版本时，必须严格按照本清单逐项核对并执行。

### 1. 编译与打包前置技术清单：
*   **前端独立编译**：必须在打包前运行 `pnpm web:build`（`tsc --noEmit && vite build`），严防 `tauri build` 直接打包过期 dist 资产。
*   **构建输出检查**：打包脚本（`pnpm portable`）必须在前序编译进程完全退出且 Exit Code 为 0 后启动。

### 2. CI/CD 与 API 限制规避流程：
*   **Draft Release 404 规避**：查询 Release 必须使用 `listReleases` 接口拉取 Release 列表并匹配 `tag_name`，禁止根据 Tag 直接查询 Draft 状态的发布版。
*   **Linux 构建兼容**：Actions 中 Linux 平台编译必须强制禁用 `includeUpdaterJson`，防止签名缺失报错。

### 3. 安全隔离与凭证保管规程：
*   **调试隔离（Scratch）**：所有包含敏感密钥/Token 的辅助发布脚本，统一放在已写入 `.gitignore` 的 `scratch/` 目录下，严防推送至公共仓库触发 Push Protection 拦截。
*   **静默封装**：自动化发布任务中，可能导致 AntiGravity 弹窗审批的命令（如 git 远程推送、脚本部署），必须使用 `powershell -Command` 进行静默封装。

## 📋 七、 开发期特定技术故障修复 SOP

本章节定义了开发测试期间常见环境与流程故障的标准修复手段。

### 1. Windows 全局服务锁定（os error 32）修复流程：
*   当工作空间下的 `clash-verge-service.exe` 被 Windows 全局服务锁定导致无法编译时：
*   **SOP 步骤**：引导用户打开日常使用的“正式版客户端”，在设置中点击“重新安装/修复服务”，将全局服务的执行路径强行重新指回安装目录（Release 目录）。此时开发目录的文件锁会自动释放。

### 2. 本地运行 Webview2 Updater 日志报错修复流程：
*   当本地运行 `pnpm updater` 报错上下文丢失或日志解析崩溃时：
*   **SOP 步骤**：
    1. 使用 PowerShell 显式定义并注入环境变量：`$env:GITHUB_REPOSITORY = 'qiu-yuxiao/clash-mini'`。
    2. 确保在 `updater-fixed-webview2.mjs` 中添加对 `resolveUpdateLog` 的异常捕获兜底链，防止 `Changelog.md` 缺少当前版本号时脚本崩溃。
