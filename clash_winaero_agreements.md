# Clash WinAero 权威开发协议 (Authorized Development Agreements)

本协议是 Clash WinAero 项目开发与优化的唯一权威指南。所有功能开发、界面微调和行为逻辑必须严格遵守本协议。本协议将随代码一同进行 Git 版本管理。

### 🚨 【修改顺序铁律与避坑把关（开发前必读）】
**任何针对本项目的功能开发、Bug 修复、UI 细节微调或系统配置变动，必须遵循「协议与避坑查阅优先」的硬性流程：**
1. **查阅红线**：每次开发前，必须先查阅项目避坑红线文件 [clash_winaero_pitfalls.md](file:///c:/Users/sun_y/Documents/AntiGravity_Projects/ClashVerge/clash_winaero_pitfalls.md)，确保绝对不触犯静默运行命令等底线错误。
2. **先改协议**：在触碰任何代码（TS/React、Rust、JSON 等）前，必须首先在《唯一协议》（本文件）的对应章节中，将相关修改的设计规范、端口分配或行为变更写入协议。
3. **后改代码**：保存协议更新后，方可编写代码。代码的实际实现必须与更新后的协议条款保持 100% 吻合。
4. **流程审计**：任何未在协议中备案的代码修改，或未经用户在对话中明确允许而执行的代码操作，均视为严重违规。

---

## ⚙️ 一、 项目定位与系统隔离规范

为了保证 Clash WinAero 与原版 Clash Verge 在同一台 Windows 机器上**完美并存、互不干扰、流畅运行**，必须在前端和打包配置中实施彻底的命名和配置隔离，严禁触碰 Rust 后端核心。

1. **开发边界**：
   * 主要针对**前端（TypeScript/React）**和打包配置进行修改。当前后项需求产生冲突时（例如第四章托盘图标规范、第五章第15项移除托盘轻量模式等涉及后端的后发需求），遵循**后项规定优先（后方优先）**方针，允许对 Rust 后端代码（如 `mod.rs`、`menu_def.rs` 等）进行局部调整与重构，但仍需确保不修改后端核心业务架构与内部稳定性。
2. **显示名字与内部代号**：
   * 前端所有面向用户的展示界面，软件名称必须精确显示为：**`Clash WinAero`**（注意大小写和空格）。
   * 程序打包（如 `tauri.conf.json` 中的 product name）也必须使用精确的 `"Clash WinAero"`。
3. **数据与配置文件隔离**：
   * 必须保证 Clash WinAero 与原版的配置相互独立。配置文件（如 `clash-verge.yaml`）、数据库以及订阅列表（`profiles.yaml`）在用户本地 of `AppData\Roaming\clash-verge` 目录中独立加载（沿用原版后端逻辑，以防破坏后端稳定性）。
4. **网络端口避让**：
   * 默认 Mixed 混合代理端口设为 `10801`，Controller API 端口设为 `9098`，彻底避让原版默认端口，允许两个客户端同时在线。
5. **系统服务共享机制**：
   * 保持系统服务注册名 `clash_verge_service` 不变。新用户启动 TUN 模式时自动触发 UAC 安装该服务；若原版已安装，将通过 SCM 共享管道接管服务运行独立的 Clash 核心配置，实现无缝切换。
6. **界面语言单一化**：
   * 软件界面一律仅保留简体中文，彻底废除多语言切换选择框与非简体中文语言资源。
7. **禁用自动更新**：
   * 彻底禁用检测及提示官方 Clash Verge 新版本的 updater 自动更新机制与接口，防止用户因升级覆盖而损坏项目独立性。
8. **单实例检测端口隔离**：
   * 为了彻底实现多客户端完美并存运行，Clash WinAero 的单实例检测端口在 Release 模式下设为 `33335`，在 Dev 模式下设为 `33336`。绝对禁止使用与原版相同的端口（`33332`/`11234`），从而彻底杜绝因单实例检测机制互锁而产生的冲突和闪退。

---

## 🎨 二、 界面布局与视觉规范

主界面窗口必须采用紧凑扁平的精致质感，拒绝粗大臃肿 of 现代 UI。

1. **双层常驻布局**：
   * 界面不设侧边栏/多页面导航。窗口常驻分为上下两层：
     * **上半层（自适应高度，flex: 1）**：展示“当前活跃节点”以及下方的“节点与延迟测试列表”。
     * **下半层（固定 178px 高度，常驻）**：展示全宽的流量监控折线图与指标数据。该固定高度确保流量折线图能够完全恢复为原版 Clash Verge 的 130px 物理高度，彻底解决高度压缩导致底部时间轴文本与“Smooth”和“Points”等状态文本发生重叠的问题。下方的四个数据指标卡片设计为横向单行排列，且采用微凸起、立体按钮感（高 24px，圆角 6px，带有上边缘微高光与外阴影投影）的精致卡片设计，并拥有与折线图曲线颜色相呼应的背景与边框色（上传指标为淡琥珀金色调，下载指标为淡电光蓝色调）。
2. **滑出设置面板**：
   * 点击右上角齿轮图标，设置抽屉往左下方滑出，**刚好完全覆盖上半层区域**，而**下半层（178px 区域）的流量与图表依然露出来并保持可见和高频更新**。
   * 设置抽屉子窗口的边框采用 **`4px double`** 双实线设计（`border: "4px double var(--aero-border)"`），与主界面节点表格的中缝双线相呼应。
   * 设置面板内采用**左右分栏**布局：
      * **左侧分栏（固定为 200px 宽度）**：自上而下整齐摆放「机场订阅区」、「接管模式选择」与「极简设置项」，不随窗口宽度变化拉伸。
      * **右侧分栏（自适应 flex: 1 宽度）**：完整放置「手动路径控制（原连接审计中心）」，随着窗口宽度自适应拉伸。
3. **原生标题栏与避让**：
   * 窗口必须保留 Windows 系统的原生边框和标题栏 (`decorated: true`)。为了在原生标题栏中展现粗体，强制使用数学无衬线粗体字符 `𝗖𝗹𝗮𝘀𝗵 𝗪𝗶𝗻𝗔𝗲𝗿𝗼` 写入 Tauri 窗口的 title 属性。
   * 设置齿轮按钮以绝对定位放置在客户区最右上角，宽度 and 高度固定为 `36px`，与上方的原生标题栏底部及右侧窗口边缘保持零间距贴合。
   * 置顶的「当前活跃出口节点状态显示条」右侧必须增加间距（`marginRight: "44px"`），防止其与右上角设置按钮发生重叠。
4. **字体与字号**：
   * 全局字体统一使用 **微软雅黑 ("Microsoft YaHei")**。
   * 字体小巧精致：主字号设为 `12px`，辅助小字号设为 `10px/11px`。关键数值（如延迟、速度、标题）偏好使用 **粗体 (Bold, 字重 600/700)**。
5. **全局 Aero 拟物效果彻底清除与扁平化重构**：
   * **清除拟物特效**：彻底禁用和清除所有拟物化视觉特效，包括按钮/卡片的渐变高光层（sheen）、悬停时的对角线扫光动画、文字阴影（text-shadow）、水晶发光阴影及呼吸灯闪烁动画（如 `aero-glow-pulse`），确保界面完全呈现出平整实体、现代扁平化（Flat Design）的简约风格。
   * **禁用窗口透明与虚化**：在 Rust 窗口配置（`window.rs`）中将 `transparent` 设为 `false`，彻底注释/移除 `apply_mica`、`apply_acrylic` 和 `apply_blur` 等窗口虚化效果的注入，确保原生窗口完全不透明。
   * **清除虚化与半透明**：彻底清除所有 CSS/SCSS 样式文件以及 React 组件属性中的虚化滤镜（包括 `backdrop-filter`、`-webkit-backdrop-filter` 以及 React 的 `backdropFilter` 属性）。废除所有半透明（如 `rgba(...)` 或 `alpha(...)` 比例）悬浮及面板背景设定。将主背景（`--aero-bg`）、设置抽屉与面板（`--aero-panel-bg`）、下拉菜单、弹出对话框、右键上下文菜单及详情面板的背景色全部提升为 **100% 实体纯色不透明**（例如亮色模式使用 `#ffffff` 或 `#f0f5ff`，暗色模式使用 `#1e2438`）。
   * **保留经典分割线结构**：特别保留用于结构分隔的表格 2px 网格线、第一列右侧中缝的 5px double 双实线边框、表格外周的 4px 粗线，以及设置抽屉的 4px double 双实线边框，仅剥离其表面的拟物发光和透光特效。
6. **流量折线图配色、尺寸同步与防挤压优化**：
   * 流量监控折线图（Canvas 渐变绘制）的上传（琥珀金 `#D4AF37`）与下载（电光蓝 `#0084FF`）必须采用标准十六进制颜色。禁止在动态生成渐变时使用 rgba 字符串做拼接，防止由于 alpha 字段拼接不规范导致 Canvas addColorStop 抛出 SyntaxError 闪退。
   * 为了防止图表在窗口尺寸变化或设置抽屉开关过程中因为渲染休眠（即 `dataStale` 或 `windowFocused` 判定）导致 Canvas 未及时重置尺寸从而被浏览器强行拉伸（如时间轴文字呈现出“瘦长”的异常长宽比），必须简化渲染判定逻辑，仅在 Document 处于 Hidden 状态下休眠。只要窗口可见，即使数据暂时没有更新或窗口未激活，在发生大小变化或重绘调用时也必须保证 Canvas 尺寸与 CSS 属性实时同步。
   * 为了防止流量图表被挤压得过扁、视觉体验不佳，必须收紧 Canvas 图表的上下左右内边距：顶部边距设为 `10px`，右侧边距设为 `10px`，底部边距设为 `22px`，左侧边距设为 `30px`；时间轴 Y 轴位置自适应调整为 `height - padding.bottom + 12 = height - 10`。这样既能保证时间数字和刻度正常显示，又能最大化折线图本身的可用高度。
7. **输入框组件细节微调**：
   * Mixed Port 端口输入框：强制使用 text 类型，宽度设为 `60px` 以隐藏浏览器原生数值增减小箭头，输入字号为 `11px`，描述文字为 `12px`。
   * 订阅链接输入框：高度设为 `26px` 以与右侧“导入”按钮等高对齐，Placeholder 提示文字修正为 `"填入订阅链接 (YAML)"`，消除拼写重复。
8. **基础设置模块重命名与排版调整与外观主题**：
   * “基础与自适应设置”卡片重命名为“基础设置”。子项 Typography 描述字体调大到 `13.5px`，端口输入字号调为 `12px`，且将系统运行日志查看整合为居中的“系统调试运行日志”主按钮（Contained 样式，高度 26px）。
   * **外观主题切换**：在基础设置列表中新增“外观主题”选项，右侧渲染 22px 高度的精致 Select 下拉选择框，支持“系统默认”(system)、“浅色模式”(light)、“深色模式”(dark) 三者切换，允许用户热切换整体界面风格。
9. **主界面节点列表（双列表格）网格与字体设计**：
    * 表格高度与布局：保持每行 2 个节点的紧凑布局，单个节点高度固定为 `20px`。
   * 字体大小调整：节点列表中的字体大一号，即节点名称字体大小设为 `12px`（原为 `11px`），协议/类型及延迟数值的字体大小设为 `11px`（原为 `10px`），选中时名称字体加粗。
   * 网格线加粗：表格内部的所有网格线（水平分隔线、垂直列格线）宽度翻倍，设为 `2px solid`（即比原本的 `1px` 增加一倍）。
   * 左右纵向双线划分：为突出左右两个节点分列的对称感觉，在一行左右两列正中间（即第一列右侧），使用宽度为 `5px double` 的 vertical 双实线边框（`(theme) => 5px double ${theme.palette.divider}`），其颜色与内部网格线完全一致，实现明显的左右两半隔断感。
   * 延迟列居中对齐与宽度：延迟数值那一列（Column 3）的宽度由原本 of `50px` 调整为 `65px`，以完全容纳 `"Timeout"` 和 `"testing"` 等长文本。延迟列的对齐方式改为居中对齐（`justifyContent: "center"`），以解决 `"Timeout"` 文本偏左溢出的视觉缺陷。
   * 外周粗框线：整个节点列表表格的外周包裹线（Box 容器边框）线宽设置为原本的 4 倍，即 `4px solid`，以此突出这是一个完整的 Excel 网格化表格。

---

## ⚙️ 三、 路由逻辑与「手动路径控制」

1. **接管模式二选一**：
   * 简化复杂的开关组合。接管方式为互斥的“系统代理 (System Proxy)”与“TUN 虚拟网卡”二选一单选组。
2. **智能规则分流**：
   * 智能规则分流 (Rule) 作为唯一核心默认工作模式。全局代理 (Global) 和完全直连 (Direct) 降权为临时排错手段，从主界面核心区移除。
3. **GFWList 规则源与强规则置顶 (GEOSITE Rules & GFWList Fallback)**：
   * **强规则置顶**：为了 100% 避免国内 DNS 污染并保证核心服务绝对可用，在规则最顶部强制注入 Clash 内核自带的 `GEOSITE` 强域名解析规则，包含 `GEOSITE,google,PROXY`、`GEOSITE,github,PROXY` 和 `GEOSITE,telegram,PROXY`。这些服务直接通过域名匹配走代理，绕过本地 DNS。
   * **GFWList 兜底**：使用自动更新 of GFWList 规则集（从 Loyalsoldier 的 `gfw.txt` 每日拉取更新，更新周期 `86400` 秒）作为其他被墙网站的拦截与路由兜底，命中后走 `PROXY` 组。
   * **国内流量直连**：规则最末尾使用 `MATCH,DIRECT` 确保未匹配到上述规则的国内流量默认直连，省流量且国内网速最大化。
4. **手动路径控制（连接管理）双列设计与滚动条高度约束**：
    * **表格双列裁剪与比例伸缩**：为了在有限 of 面板宽度中提供最直观的路由分流信息，并防止多列引起的水平溢出遮挡垂直滚动条，**彻底裁剪该表格至仅保留「链接目标 (Host)」与「路由 (Chains)」两列**。其他多余列（如流量、速度、应用进程、规则等）一律移除。在「路由 (Chains)」列中，**不显示具体的代理节点路径，而是仅过滤提取并展示该连接对应为「直连」、「代理」或「封锁」**三者之一的直接模式。**「路由」列采用固定宽度布局（固定为 50px，始终贴 in 最右侧，不随窗口变化拉伸），其数据内容与表头“路由”字样一律设为水平居中对齐（`align: 'center'`），左侧的「链接目标」列则采用 `flex: 1` 占满剩余所有空间并随着窗口宽度自适应拉伸**。这确保了用户在任何窗口尺寸下都能完美查阅路由结果，且彻底避免水平滚动条。
    * **表格风格**：超紧凑 Excel 风格，行高固定为 `20`，单元格 py 内边距收窄为 `0.5`，文字字号 `12px`，单元格左右边距 `1.5`（即 12px）。外边框与内部的行/列网格线全部重构为 **`2px solid`** 实体分割格线，以展现清晰网格感。奇偶行使用淡色调（如微米蓝色调）的斑马纹，禁止使用单调的灰度差。
   * **滚动与高度约束**：手动路径控制的表格外部容器（在 `_layout.tsx` 中）以及其父容器「右侧路径控制分栏 (Right Connections Column)」必须设置 `minHeight: 0` 和 `overflow: "hidden"` 属性，以确保整个分栏高度受到滑动抽屉（aero-panel）的可战高度约束，防止表格伸长溢出而导致滚动失效。同时，为了满足界面美观且无冗余视觉元素，将表格的滚动条完全隐藏（使用 `scrollbarWidth: 'none'` 及 `&::-webkit-scrollbar { display: 'none' }`），但保持鼠标滚轮与触控板的上下滚动功能完全正常。
   * **快捷控制**：右键点击任意连接，弹出快捷分流上下文菜单，选项为：
     * **走直连 (DIRECT)**
     * **走代理 (PROXY)**
     * **封锁它 (REJECT)**
     点击后将该域名/IP 注入到最高优先级分流策略中，立竿见影地修正或补漏规则。
5. **唯一代理组与去机场规则化 (Single Proxy Group & Anti-Airport Rules)**：
   * **去机场分组与规则**：Clash WinAero 彻底摒弃并过滤机场订阅配置文件中自带的、复杂且不透明的多级代理分组（如 YouTube、Netflix、Google、Apple 等）和非公开分流规则，防止其消耗额外流量或引流敏感数据。
   * **唯一 PROXY 组**：底层的 Clash 配置只保留一个唯一的代理组（命名为 `PROXY`），该组包含且只包含解析出来的原始服务器节点。
   * **主界面节点表格**：主窗口上半层直接展示这一个唯一的 `PROXY` 组下的所有原始服务器节点（不再显示任何代理组分类 and 多标签）。用户在表格中选中某一个节点，即代表当前代理出口切换为该节点，所有走代理的流量均以此节点为唯一出口。

---

## 🐱 四、 系统托盘图标规范

系统托盘图标使用指定的布偶猫咪头像图片，并直接从后端打包嵌入 PNG 字节流，以确保和原版彻底隔离：

1. **普通模式 (Common / 不代理或直连模式)**：
   * 采用金属质感灰色猫咪头像：`clash_winaero_ragdoll_metal`（`tray-icon.png`）。
2. **TUN 虚拟网卡模式**：
   * 采用带星星发光质感的蓝色猫咪头像（`tray-icon-tun.png`）。
3. **系统代理模式**：
   * 采用带金色相框的猫咪头像（`tray-icon-sys.png`）。

---

## 📋 五、 17 项用户反馈问题状态跟踪清单

| 编号 | 反馈问题描述 | 状态 | 最新处理与对齐方案说明 |
| :--- | :--- | :---: | :--- |
| **1** | 项目是否使用了 Git 进行版本管理？ | **已解决** | 本地已完全使用 Git 托管，当前在 `dev` 分支。远程 `origin` 仅用作拉取官方更新。 |
| **2** | 应用版本号问题：当前版本为 2.5.2，建议从 0.0.1 开始。 | **已解决** | 前端发布包版本号已重置为 `0.0.1`，下一个修改发布的版本为 `0.0.2`。 |
| **3** | 流量折线图图表一片空白，下方四个指标颜色杂乱。 | **已解决** | 移除了 Web Worker，改为 React 状态直接 Canvas 渲染。上传指标统一为淡金色/琥珀色，下载指标统一为标准电光蓝/青色，移除拟物光感。 |
| **4** | 「基础与自适应设置」滑动开关选项图标过大。 | **已解决** | 全局 Switch 物理尺寸固定缩减为小巧精致的规格（宽 `32px`，高 `18px`，滑块 `14px`）。 |
| **5** | 下拉菜单与抽屉面板背景透明度过高，导致文字重叠混杂。 | **已解决** | 全局下拉菜单、抽屉、主背景以及全部拟物效果彻底清除，不透明度提升至 100% 纯色，以解决渲染沉重和文字穿透问题。 |
| **6** | 界面词汇翻译生硬，例如“混合监听端口”。 | **已解决** | 统一替换为更加专业且通用的英文标准术语：**`Mixed Port`**。 |
| **7** | 主窗口上半层节点列表展示方式应改成 Excel 紧凑双列表格（选中半行）。 | **已解决** | 彻底废除单列和状态对勾列，重构为每行 2 列（每行两个节点），每列展示“名称、协议、延迟”三项要素，高度为 `20px`。每个节点直接以 `ListItemButton` 渲染。选中节点时仅高亮对应的半行单元格。加入微米淡蓝色调的行级斑马纹。表格外周框线加粗至 4px，内部网格线加粗至 2px，第一列右侧中缝分隔线采用 5px double 且与网格同色，延迟列宽度调至 65px 并居中对齐，字体大一号。 |
| **8** | 设置面板右上角组件重叠；连接的“活跃/历史”按钮位置不合理。 | **已解决** | 1. 设置抽屉打开时自动隐藏主窗口右上角齿轮。 2. “手动控制路径”的“活跃/历史”按钮与标题同行并排靠左对齐，防止低分辨率/窄屏时重叠纠缠。 |
| **9** | 「查看系统运行日志」按钮排版与周边格格不入。 | **已解决** | 取消右下角/底部突兀的按钮与冗余行，在「基础设置」卡片最下方直接渲染为居中的“系统调试运行日志”主按钮（Contained 样式），消除冗余文本描述。 |
| **10** | 规则模式下无法代理 Google 等常用站点。 | **已解决** | 在 GFWList 模式最顶部强制注入 GEOSITE 强规则，绕过 DNS 污染，并在背景自动更新。 |
| **11** | 界面术语优化与对称概念命名。 | **已解决** | 1. 将“分流策略”文案改写为“代理策略”；2. 将“直连”模式在代理模式对比命名中改写为“全局直连”，与“全局代理”对称。 |
| **12** | 「手动路径控制」列表排版空旷，需要收紧行距并统一风格。 | **已解决** | 将表格收紧，统一为 Excel 精致表格（12px字体，1.5倍边距，外框线与单元格网格线），奇偶行使用与节点列表一致的淡蓝色斑马纹；右侧滚动条移除本地淡色样式覆盖，直接继承并使用与主界面全局完全一致的 scroller 滚动条样式。 |
| **13** | 连接审计中心行点击详细信息面板定位 Bug（不随行滚动）。 | **已解决** | 点击行时的详细信息抽屉已被修复，能正确定位。 |
| **14** | 连接审计中心功能引导与路由设置说明（右键快捷分流）。 | **已解决** | 重命名界面标题为“手动控制路径（右键点击链接）”，右键菜单中增加走直连、走代理、封锁它的快捷分流指令并注入最高优先级规则。 |
| **15** | 系统托盘右键菜单中保留的「轻量模式」选项点击无响应。 | **已解决** | 从系统托盘 of 右键上下文菜单配置中，彻底移除「轻量模式」这一行选项。 |
| **16** | 主页面置顶的已选中节点信息显示条布局分裂，左右留白过多。 | **已解决** | 取消原左右两侧对齐的设计，将所有文字信息和延迟数据统一在一行内**居中对齐排列**。 |
| **17** | 订阅列表中出现没有名字的空行，删除重启后还会再现。 | **已解决** | 在前端过滤掉没有名字（即 `name` 为空或纯空格）的订阅项，确保后台自动生成的空模版配置不会显示在前端界面。 |
