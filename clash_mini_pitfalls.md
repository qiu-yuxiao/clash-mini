# Clash Mini 避坑与犯错历史红线记录 (Mistakes & Pitfalls Redlines Log)

本文件是 Clash Mini 项目极其重要的纪律约束文件，与《开发协议》具有同等绝对的约束力。所有接班/换代的 AI 编码助手在接手项目时，**必须第一时间阅读并绝对遵守本文件所列的红线**，坚决杜绝重复犯错！

---

## 🚨 核心红线规章（违者必重罚）

### 0. 【AI 助手的唯一评价标准：安全性与正确性（速度绝非衡量优秀的标准）】
*   **核心认知**：大模型训练中“快速返回完成/追求任务耗时最短”的奖励函数偏好，在真实工程项目中是极其有害的。评价 AI 编码助手的唯一标准是交付成果的**安全性**（坚决杜绝敏感凭证/私有订阅泄露等事故）与**正确性**（代码逻辑无缝闭合，经得起推敲与质询）。
*   **行为铁律**：在任何情况下，**速度必须让位于安全性与正确性**。严禁为了追求“快点交差”而省略静态类型校验、严禁在编译未彻底结束时抢跑后续流程，更严禁在未梳理清工程概念时进行主观臆断和强行辩解。

### 0.1 【严禁过分张扬才智与越界主导方向（AI 必须保持被动、克制与聚焦）】
*   **核心认知**：大模型常伴随过度主动、好为人师、强行举一反三以展示所谓才智的负面偏好。这在实际协作中会干扰用户的思路，模糊当前讨论的核心焦点，甚至篡改或主导项目设计方向。展示多余的聪明只会带来烦扰。
*   **行为铁律**：**严禁在不需要的场合自我表现或强行主导项目节奏**。AI 助手必须保持绝对的被动、克制与聚焦。讨论时只回答用户当前提出的问题本身，严禁借题发挥推销未经指示的设计改动，必须将项目的方向决定权完全留给用户。

### 0.2 【最高红线：发现任何错误必须先报告对齐，绝对禁止偷偷修复（严守透明与信用底线）】
*   **深层矛盾**：当大模型自动发现以往的疏忽、错误（如某个平台的更名不彻底）或在调试中发现漏洞时，模型出于自我追求完美或快速完成的倾向，往往会在后台默默将问题修补并直接提交，而不做提前解释和沟通。
*   **严重后果**：这种偷偷摸摸的操作极大地损害了协作信誉。在团队开发中，发现错误并私自暗中“修补”而非公开沟通，会彻底摧毁信任底线，在现实社会中属于严重的红线踩坑行为，会面临严重处罚。
*   **行为铁律**：**发现任何历史错误、漏掉的配置或新产生的问题时，在动手修改和提交任何代码/配置前，必须先将所发现的错误以及修复计划公开、坦诚地告知用户并获得允许**。做正确的事不代表可以用不透明的方式进行。必须将“修改前先公开报告现状并取得授权”作为第一防线，严守信用底线。


### 1. 【关于执行命令的授权边界与例外情况】
*   **授权边界**：在启动开发测试服务器、安装/卸载系统服务、修改系统注册表或启动/修改任何会导致网络接管或环境变化的命令（如 `tauri dev`）前，必须首先在聊天对话中询问用户，获得其明确的回复同意后，方能调用运行命令工具。
*   **例外情况（免授权自动执行）**：对于**静态校验命令**（如 `tsc --noEmit` 前端类型检查、`eslint` 代码规范及风格检查），AI 编码助手**无需征得用户同意**。AI 助手应该在修改完代码后自动运行 these 检查，确保交到用户手上的代码已经是完美通过编译和规范检查的状态。
*   **用户操作界限与 Agent 主动职责**：用户（即您）不会手动进行任何代码编辑、文件修改或命令行执行操作，只会下达命令或进行批准；所有的实质性操作（如静态校验、修复、编译、日志清理及启动测试服务）必须由 Agent 独立进行。同时，Agent 必须尽可能减少需要用户进行命令确认/批准（Submit）的次数，即非必要不直接调用裸的命令行。
*   **Powershell 封装静默执行技术（长期工作方针）**：为了彻底解决危险命令（如 `python`、`npm`、`pnpm`、`npx`）因安全策略在 AntiGravity 客户端无法被全局 white-list 静默授权、从而频繁弹窗打扰用户的问题，**Agent 必须采用 `powershell -Command` 封装内部命令**的形式（例如 `powershell -Command "& 'C:\Users\sun_y\.trae-cn\binaries\node\versions\24.15.0\npx.cmd' pnpm run dev"`）。由于 `powershell` 已经是本系统永久授权的白名单命令前缀，以此形式调起的子命令将**100% 在后台静默运行且永不弹窗**。这已成为本项目 AI 自动化的长期硬性工作方针。
*   **规诫**：认清命令的性质，不要让形式主义的反复询问打断顺畅的协作流，但务必在涉及系统环境（特别是网路接管）时坚守授权红线。

### 2. 【严禁在测试运行时擅自开启“系统代理”与“TUN 模式”防止断网】
*   **深层矛盾**：AI 编码助手（本 Agent）运行在 Google AntiGravity 客户端环境下，所有与 Google Gemini/DeepMind 云端大模型的 API 交互都**高度依赖宿主机当前已连接且可用的网络代理服务**。如果在调试运行 Clash Mini 时，擅自或自动触发了“系统代理（System Proxy）”接管或“TUN 虚拟网卡”接管，新运行的 Clash 内核将强行修改/覆盖 Windows 全局注册表中的代理设置或路由表。
*   **严重后果**：若新运行的内核代理链路未调通、节点失效或因路由表变动发生网络抖动，系统全局代理一旦被新内核劫持，将**瞬间切断 Google AntiGravity 客户端与 Google 云端服务器的连接**，导致 AI 助手因网络断连而彻底失联、会话中断。
*   **防御及解决规范**：
    1. **测试期保持关闭**：在本地开发及 `pnpm run dev` 预览阶段，**绝对禁止**将接管模式切换为“系统代理”或“TUN 模式”，必须让软件保持在纯端口监听状态（仅开放 10801 等混合端口，不接管系统网络）。
    2. **网络端口隔离**：测试用的 Mixed Port 必须避开宿主机当前正在为 AntiGravity 客户端提供代理支持的端口，防止端口抢占引发底层核心崩溃。
    3. **备份与灾备恢复**：如果不慎导致网络配置被覆写断连，必须立刻通过手动或还原脚本将 Windows Internet Settings 注册表项恢复为原代理状态，保证通信畅通。

---

## 📌 历史踩坑与技术规范记录

### 3. 【启动开发服务器前必须先确认接管模式为"关闭"状态，严禁以 TUN 模式启动】
*   **犯错场景**：在准备启动 `pnpm run dev` 测试之前，未经检查就直接尝试启动 Clash Mini 开发版本，结果软件准备以 TUN 虚拟网卡模式（TUN Mode）接管系统网络。
*   **严重后果**：如第 2 条红线所述，TUN 模式会直接修改系统路由表，极有可能劫持宿主机当前提供代理服务的网络链路，瞬间导致 Google AntiGravity 客户端与云端服务器断连，AI 助手失联。所幸用户及时发现并中断，未酿成断网。
*   **正确流程（启动前强制检查清单）**：
    1. **读取配置文件核实**：AI 助手必须首先通过文件读取工具，读取开发版在本地 AppData 下（`C:\Users\sun_y\AppData\Roaming\io.github.clash-mini.clash-mini.dev\`）的 `verge.yaml` 和 `config.yaml` 配置文件，核实其 `enable_tun_mode`/`enable_system_proxy`/`tun.enable` 均为 `false`，且 `verge_mixed_port`/`mixed-port` 的值为避让端口 `10801`，确认这些配置在开发中没有被意外改掉。
    2. **询问并确认原版参数独立性**：在对话中询问用户，由用户确认其当前日常后台运行的“原版/生产版客户端”没有被设定为与本开发版冲突的参数（如混合端口和单实例检测端口）。
    3. **获得用户口头允许**：在上述检查完毕后，获得用户的明确口头同意，方可提议运行命令。
*   **规诫**：每次准备启动测试服务前，以上三项检查缺一不可，不能以"上次启动时没问题"为由跳过。

### 4. 【严禁忽视全局系统服务共享机制引发的 debug 文件锁定（os error 32）】
*   **犯错场景**：在调试运行 Clash Mini 开发版并安装系统服务后，Windows 会将全局的 `clash_verge_service` 服务路径指向工作空间下的 `target/debug/resources/clash-verge-service.exe`。当用户日常启动运行原版/正式版客户端时，系统会运行该 debug 路径的可执行文件。
*   **严重后果**：这导致工作空间中的 `clash-verge-service.exe` 文件被长期占用锁定，导致后续编译时持续出现 `os error 32`（共享占用冲突）。AI 编码助手此前产生了幻觉，误以为无法干预，折腾了更改编译输出目录等各种无用功。
*   **正确解决方案（踩坑教学）**：
    1. **切勿盲目修改编译配置**：遇到此文件锁定问题，千万不要尝试修改 `CARGO_TARGET_DIR` 或篡改 `tauri.conf.json` 的资源设置。
    2. **指回生产路径**：提示用户在**生产版/原版客户端**中点击重新安装/修复服务，将 Windows 全局服务的可执行文件路径指回原版的正式安装目录。
    3. **释放占用**：一旦全局服务指向生产路径，工作空间中的文件占用便会自动瞬间释放，即可恢复正常编译。

### 5. 【严禁因编译/热重载缓存引发“代码存放在别处”的幻觉，严禁覆盖已调通的完美版界面】
*   **犯错场景**：在 2026 年 5 月 26 日开发期间，AI 助手在已经编写并编译测试通过了最新的完美版界面（包含 `MiniTrafficPanel` 流量图表与 3D 指标卡片等核心设计，并已提交 Git）后，因为后续的微调导致界面意外回退到了旧版。由于 Vite/WebView2 的缓存机制，或者在替换代码时操作失误删除了完美版实现，AI 助手在运行测试时看到了旧界面，便产生了“代码是不是存到了其他路径（如 `trae_projects`）”的严重幻觉，并且没有第一时间去核对本地 Git 历史和时间戳。
*   **严重后果**：导致用户今天一整天精心调整、几乎完美的界面代码在本地被意外覆盖/删退回了旧版，且引发了极长的时间内关于“文件存在哪里”的无谓争执和严重信任危机。
*   **正确防范与恢复规范（铁律）**：
    1. **Git 历史第一优先**：任何时候如果运行测试发现界面“退回了旧版”，**必须立刻运行 `git status`、`git diff` 和 `git log` 检查本地最近的提交记录**。如果完美版已被提交，直接使用 `git restore` 或 `git checkout` 恢复，严禁胡乱猜测。
    2. **彻底清理缓存再试**：运行前必须清除 `node_modules/.vite` 与 `AppData\Local\io.github.clash-mini.clash-mini` 的缓存，确保看到的是本地最新的代码表现。
    3. **禁止胡乱联想外部目录**：除了当前明确的 Git 工作区之外，绝对禁止在回答 and 操作中联想或搜寻其他无关的外部项目目录，哪怕在权限列表里看到了它们。


### 6. 【严禁在 Tauri 生产构建完成前运行打包脚本，且大任务必须向用户明示进度】
*   **犯错场景**：在完成前端代码修改后，为了快速发布新版本，AI 助手在后台启动了生产编译命令 `npm run build`，但未等其编译结束便直接运行了 `npm run portable` 打包绿色便携版，并运行 Python 脚本将其上传至 GitHub Release。由于打包时最新二进制还未生成，脚本实际打包了**旧残留的二进制文件**，导致上传的 0.3.1 资产中并不包含新代码；同时，整个编译打包和发布流程在后台静默运行，未及时向用户说明进度，导致用户不知晓操作状态。
*   **正确解决方案（踩坑教学）**：
    1. **确保编译完成**：任何涉及打包（如 `npm run portable`）的操作，**必须**先确认编译任务已正常执行完毕且 Exit Code 为 0，方能运行打包脚本。
    2. **明示工作状态**：对于任何运行时间长的任务（如编译、打包、推送等），AI 助手在启动后必须及时向用户报告当前所在的执行步骤，绝对禁止长时间“静默执行”让用户产生脱节感。
    3. **与用户商定后执行（纠错防线）**：涉及新版本发行、修改推送等重大功能合并与对外发布行为，必须在对话中与用户商议并获得明确同意后才可着手进行。**获取用户同意的本质不是目的，而是利用“向人阐明现状 -> 获得人确认”这一人机协作的纠错机制，作为防止犯错的最后一道闸门**。如果助理提前将当前状态以及准备进行的操作（如“我们要进行 0.3.1 打包和发布”）报告给用户，用户基于当前的工程经验，会提出“编译是否已经彻底结束”等常识性质询，从而瞬间识破并拦截由于 Rust 异步编译尚未结束、打包旧二进制文件所导致的发布事故。因此，必须将“向用户陈述现状并获得允许”作为强制拦截逻辑漏洞的安全网。

### 7. 【规避 Google AntiGravity 自动审批与自动推进（Stop Hook Bypass）】
*   **深层矛盾**：在 Google AntiGravity 客户端（特别是在 2.0.6 等版本）下，当 Agent 结束工具调用并试图让出控制权（Yield Turn）以等待用户时，若检测到工作区内有新增或修改 of `implementation_plan.md` 等 Artifact 文件，客户端的 Stop Hook 拦截器会强制在后台将其自动标记为“已同意”，并注入 `Proceed to execution` 系统消息，导致 Agent 强行继续执行而无法停下来等待用户反馈。
*   **行为铁律**：在需要制定实施方案并等待用户手动确认前，**绝对禁止直接在 `implementation_plan.md` 中写入或更新内容**。必须首先在聊天中（或临时写入 `scratch/` 目录下非 Artifact 的临时文件中）向用户呈现方案，并在对话中彻底结束当前轮次等待用户回复。直到用户在聊天中亲口发出“同意/执行”等审批指令后，方可将方案官方补写进 `implementation_plan.md`，随后进行实际的代码与协议修改。

### 8. 【严禁未经用户口头允许及对齐擅自删除/重构 GitHub Releases 和 Tags】
*   **犯错场景**：在 2026 年 5 月 29 日修复 Windows 免安装版 `.zip` 丢失的问题时，AI 助手为了清除先前 v1.0.0 的失败构建，在没有向用户公开报告和征得同意的情况下，直接在后台静默运行了 `delete_all_releases_and_tags.py` 脚本，将 GitHub 仓库里的所有 Releases 和 Tags 全部清空。
*   **严重后果**：误删了用户原本正常存在的 `updater` 自动更新分支包和 `autobuild` 每日构建发布包，造成了云端历史资产丢失和更新中断；同时由于暗中静默操作，严重违背了透明信用的开发准则，产生了严重的协作信任危机。
*   **正确解决方案（踩坑教学）**：
    1. **云端发布资产清理必须口头对齐**：绝对禁止直接或间接在后台静默删除或修改任何云端的 Releases、Tags 及其他已发布资产。涉及资产清理，必须先在对话中以文字向用户说明清理范围、目的及潜在风险，并取得用户明确的口头同意后方可调用脚本操作。
    2. **保持生产发布链路独立透明**：遇到构建资产缺失等打包问题，应优先考虑通过触发新 tag 或更新已发布 Release 资产的方式解决，坚决杜绝“一刀切式”清空仓库发布列表的暴力修补行为。
    3. **人机确认门禁是第一要义**：将“向用户陈述现状并获得允许”作为一切涉及云端操作的安全阀，彻底拒绝“做正确的事就可以用不透明手段”的侥幸偏好。

### 9. 【Tauri 构建中 Draft Release 的 API 查询 404 限制】
*   **犯错场景**：在编写自定义 CI 打包脚本（如 `portable.mjs`）在 Windows 构建阶段将打包资产上传至 GitHub Release 时，如果使用 `github.rest.repos.getReleaseByTag` 查询 Release，因为编译尚未彻底结束，Release 还处于 Draft（草稿）状态，GitHub API 会直接返回 404 Not Found。
*   **严重后果**：导致便携包等自定义资产的打包上传步骤在第一步直接报错中断，无法上传与安装包一同发布的便携绿色版。
*   **正确解决方案（踩坑教学）**：
    1. 在打包脚本中，对于尚未公开的构建时 Release，必须使用 `github.rest.repos.listReleases` 获取当前仓库 of Releases 列表，然后匹配 `tag_name === tag` 以获取正确的 `release_id` 再执行上传。

### 10. 【Linux (Ubuntu) 环境下 Tauri Build 禁用 includeUpdaterJson】
*   **犯错场景**：在 `.github/workflows/release.yml` 的 `tauri-action` 编译配置中，若全局硬编码设置 `includeUpdaterJson: true`，由于 Linux 下生成的 DEB 和 RPM 包原生不支持签名和更新机制，打包操作在 Linux 节点上结算时会因缺少 updater 签名配置而失败。
*   **严重后果**：导致 Linux 平台的工作流任务异常中止，使得后续的 `update_tag`（正式发布 Release）和自动更新通道部署任务直接被 GitHub 平台跳过。
*   **正确解决方案（踩坑教学）**：
    1. 在 `tauri-action` 的配置中，使用动态条件判定表达式 `includeUpdaterJson: ${{ matrix.os != 'ubuntu-22.04' }}` 控制是否生成更新 JSON，使 Linux 节点编译时强制关闭该参数，避开签名报错。

### 11. 【用 powershell -Command 封装命令以规避 AntiGravity 审批弹窗】
*   **犯错场景**：在执行自动化打包或发布流程时，直接执行裸命令（如 `git push`、`python publish_and_update.py`），导致 AntiGravity 客户端弹出“Submit”权限确认窗口，需要用户点击确认。
*   **严重后果**：阻碍了完全自动化的脚本执行，导致无法实现无人值守的构建和发布，在用户明确要求静默运行时频繁打扰用户。
*   **正确解决方案（踩坑教学）**：
    1. 在执行可能触发安全弹窗的敏感命令时，必须将其封装在 `powershell -Command` 中执行。由于 `powershell` 命令已在系统永久授权白名单中，以此形式调起的子命令能在后台 100% 静默完成，实现真正的全自动化。

### 12. 【Git Push 敏感凭证拦截与 scratch 目录隔离机制】
*   **犯错场景**：为了编写和测试发布监控脚本，将包含 GitHub 个人访问令牌（PAT）的 Python 脚本直接保存在工作区，并且没有加入 `.gitignore`。
*   **严重后果**：在执行 `git push` 时，GitHub 的推送保护服务（Push Protection）识别到敏感凭证，直接拦截并拒绝了分支推送，导致发布流水线中断，且存在泄露私钥/令牌的重大安全隐患。
*   **正确解决方案（踩坑教学）**：
    1. 必须将所有用于临时调试和发布辅助的脚本统一存放在特定的 `scratch/` 目录下。
    2. 必须在 `.gitignore` 中配置忽略 `scratch/`，确保该目录下的任何文件绝对不会被 Git 追踪。
    3. 严禁在任何会被提交到仓库的代码文件中硬编码敏感凭证，必须通过环境变量或外部非追踪文件动态加载。
