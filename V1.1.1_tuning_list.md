# V1.1.1 Tuning List

This document tracks all requested visual and behavior adjustments during this tuning session. These will be implemented sequentially upon confirmation, aligned with the agreement, and cleared before release.

## 📌 Tuning Items

- `[x]` 1. 窗口的最小高度增加 10 像素 (由 210px 增加至 220px)
- `[x]` 2. 调整右上角设置按钮图标：将齿轮图标尺寸调大至 28px，并将叉号图标固定为 20px，确保两者中心视觉上完全同心与协调。
- `[x]` 3. 简化当前活跃出口节点状态条：移除左侧的“协议:”与右侧的“地址:”文本前缀，只展示协议类型（如 VLESS）与具体地址信息，使版面更为清爽。
- `[ ]` 4. (挂起) 修复当前活跃出口节点 IP:Port 从未显示过的问题：目前未能成功渲染，用户选择挂起此项，待后续排查。
- `[x]` 5. 调整双滑动条样式以对齐开关风格：将滑动条的滑轨（rail/track）高度调整至 14px，圆角设为 7px，滑动圆点（thumb）直径保持 14px，实现滑轨与滑块等宽的紧凑拟物效果。
- `[x]` 6. 调整双滑动条配色：将滑动圆点（thumb）的背景色改为按钮的主色渐变（与 Primary 3D 按钮一致），而滑轨（track/rail）改为中性灰（无填充色），使配色主次分明。
- `[x]` 7. 调整三个小开关的外形与配色以与双滑动条完全统一：开关高度调整为 14px，开关圆点直径保持 14px（与轨道等宽），同时滑道尺寸与宽度均保持不变；配色上，将开关圆点改为 Primary 渐变色，轨道在开启与关闭状态下均改用中性灰，实现与双滑动条一致 of 视觉大一统。
- `[x]` 8. 调整三个小开关的滑道左右长度：宽度减少 4px（从 32px 缩减至 28px），选中移动距离对应调整为 14px。
- `[x]` 9. 将「断开全部」与「清空历史」按钮的样式由 Outlined Default 升级为与主按钮一致的 3D Contained Primary 风格（彩色 3D 渐变拟物），彻底消除视觉漏洞。
- `[x]` 10. 放大「活跃」与「历史」二选一切换按钮：移除其容器的 scale(0.85) 缩放，并将高度从 22px 增至 28px，字号从 10px 调至 12px，以契合右侧整体按钮尺寸规范。
- `[x]` 11. 重构「活跃」与「历史」为二选一滑动分段选择器（Segmented Control），与主题/接管模式等滑动指示选择器风格完全统一。
- `[x]` 12. 界面控件尺寸微型化精细调整：主按钮（含导入订阅、系统调试运行日志按钮）统一调至 24px，双滑动条调至 10px，3个三选一滑块统一调至 26px，1个二选一滑块与断开/清空按钮调至 24px，小开关保持不变。
- `[x]` 13. 统一三个三选一滑块上所有文本字体粗细：无论是否选中，选项文字一律统一设为常驻粗体（bold），与按钮保持一致。
